# Agent-Fox Memory

_838 facts | last updated: 2026-04-16_

## Gotchas

- Capability negotiation allows graceful degradation for harnesses lacking optional capabilities (resume, pre_compact_hook, effort_control), but degradation is significant for security-critical capabilities like permission callbacks. _(spec: 110_hunt_dedup_and_ignore, confidence: 0.90)_
- When adding a new method to a protocol interface (like PlatformProtocol), all existing mock classes implementing that protocol must be updated to include the new method, or type checking will fail. _(spec: 110_hunt_dedup_and_ignore, confidence: 0.90)_
  - effect: Protocol interfaces should be updated before or alongside im…
- Test comments describing intended behavior (e.g., 'orthogonal → similarity 0.0') should be validated by the actual embedder implementation; mismatches indicate test setup bugs, not implementation bugs. _(spec: 111_rich_memory_rendering, confidence: 0.90)_
  - cause: When using batch embeddings across multiple pipeline stages,…
- When implementing embedding-based filtering with a fail-open design, use orthogonal test vectors (like _SequenceEmbedder) instead of identical vectors (like _SameVectorEmbedder) to avoid masking filtering logic bugs in smoke tests. _(spec: 110_hunt_dedup_and_ignore, confidence: 0.90)_
  - effect: Embedding similarity-based filtering for suppressing Finding…
- EntityType enum values use uppercase (e.g., EntityType.FILE) but queries must use lowercase strings ('file') to match the stored values. _(spec: 111_rich_memory_rendering, confidence: 0.90)_
  - effect: When implementing rendering features with multiple test type…
- When testing async functions with property-based tests, wrap multiple calls in `asyncio.new_event_loop()` to avoid event loop conflicts between hypothesis iterations. _(spec: 110_hunt_dedup_and_ignore, confidence: 0.90)_
  - cause: Hypothesis property tests should be marked with `@pytest.mar…
- When iterating through multiple directories with overlapping spec names, check if a spec already exists in the known map before adding archived copies to prevent active specs from being overwritten by their archived counterparts. _(spec: 109_ai_fix_pipeline_wiring, confidence: 0.90)_
  - cause: When building a dependency validation map, scan both active …
  - effect: Spec dependency documentation should reference the group num…
- Circular imports should be explicitly checked when verifying public entry points to prevent runtime import failures. _(spec: 95_entity_graph, confidence: 0.90)_
- When adding a field with a mutable default (list), use `field(default_factory=list)` in dataclasses to avoid shared state across instances. _(spec: 93_fix_branch_push, confidence: 0.90)_
  - cause: When a dataclass field has a default_factory (like empty lis…
  - effect: Property-based tests should include the newly added field in…
- Field default factories (e.g., `field(default_factory=list)`) must be used for mutable defaults in dataclasses to avoid shared state across instances. _(spec: 97_archetype_modes, confidence: 0.90)_
  - cause: When adding fields to dataclasses used in property-based tes…
- The ModeConfig.templates field was previously removed but is required by spec 97-REQ-1.1 to maintain 8 override fields; it must be re-added as list[str] | None = None. _(spec: 97_archetype_modes, confidence: 0.90)_
  - effect: When adding override fields to configuration classes, ensure…
- Reviewer output must use exact field names as specified in the mode documentation (e.g., 'drift_findings', 'verdicts', 'audit', 'findings') to avoid parsing failures and expensive retry loops. _(spec: 97_archetype_modes, confidence: 0.90)_
- Reviewer workflow policies must output a single, bare JSON object as the final message with no preamble, markdown fences, or postscript; the first character must be '{' and the last must be '}'. _(spec: 97_archetype_modes, confidence: 0.90)_
- Reviewer profiles define executable workflow policies with strict output format requirements: final message must be a single bare JSON object with no preamble, markdown fences, or postscript, or else triggers expensive retry loops. _(spec: 95_entity_graph, confidence: 0.90)_
- Final reviewer output must be a single bare JSON object with no preamble or markdown formatting; the first character must be '{' and last must be '}', otherwise retry loops are triggered. _(spec: 93_fix_branch_push, confidence: 0.90)_
  - effect: Intermediate messages between tool calls may contain analysi…
- Pre-review constraints exclude grep and find commands, requiring alternative approaches like ls, cat, and git for static file analysis. _(spec: 92_transient_audit_reports, confidence: 0.90)_
- All reviewer output must be a single bare JSON object as the final message with no preamble, markdown fences, or postscript text; violations trigger expensive retry loops. The JSON must start with '{' and end with '}' as the very last characters. _(spec: 92_transient_audit_reports, confidence: 0.90)_
- All reviewer output JSON fields must use exact names as specified (audit, ts_entry, test_functions, verdict, notes, overall_verdict, summary for audit-review; drift_findings, severity, description, spec_ref, artifact_ref for drift-review), and intermediate analysis text before final JSON is discarded during parsing. _(spec: 97_archetype_modes, confidence: 0.90)_
  - cause: Final output from reviewer sessions must be a single bare JS…
  - cause: Drift-review findings use severity levels (critical, major, …
- Final output from reviewer sessions must be a single bare JSON object with no preamble, markdown fences, or postscript text; the first character must be '{' and the last '}', and violating this triggers expensive retry loops. _(spec: 97_archetype_modes, confidence: 0.90)_
  - effect: All reviewer output JSON fields must use exact names as spec…
- Reviewer JSON output must be a single bare JSON object with no preamble, markdown fences, or postscript—first character must be '{' and last character must be '}' to avoid triggering expensive retry loops. _(spec: 93_fix_branch_push, confidence: 0.90)_
- Reviewer profiles enforce strict JSON output rules: final message must be a single bare JSON object with no markdown, preamble, or postscript, with first character '{' and last character '}', to avoid triggering expensive retry loops. _(spec: 92_transient_audit_reports, confidence: 0.90)_
- Reviewer profiles (audit-review, drift-review, fix-review, pre-review) enforce strict JSON output formatting: final message must be bare JSON with no preamble, markdown fences, or postscript to avoid expensive retry loops. _(spec: 95_entity_graph, confidence: 0.90)_
- Reviewer archetype modes (pre-review, drift-review, fix-review, audit-review) are defined as executable workflow policies in markdown templates; the final output must be a bare JSON object with no preamble, markdown fences, or postscript to avoid triggering expensive retry loops. _(spec: 97_archetype_modes, confidence: 0.90)_
- The tree-sitter Dart binding is provided by tree-sitter-dart-orchard package, and the grammar module name is tree_sitter_dart_orchard (with underscores), not tree_sitter_dart. _(spec: 107_additional_language_analyzers, confidence: 0.90)_
- tree-sitter-kotlin 1.x has a grammar bug that causes false-positive has_error flags for inline class bodies; implement a parser wrapper to normalize source code before parsing as a workaround. _(spec: 107_additional_language_analyzers, confidence: 0.90)_
  - effect: Language analyzers need to extract both entities (FILE, MODU…
- Hypothesis property tests may fail with filter_too_much health check warnings; suppress this check explicitly using HealthCheck.filter_too_much in suppress_health_check parameter when filtering is intentional. _(spec: 106_nightshift_ignore, confidence: 0.90)_
- Project initialization must include an _ensure_nightshift_ignore() step and add nightshift_ignore field to InitResult to ensure the test module remains fully importable. _(spec: 106_nightshift_ignore, confidence: 0.90)_
  - cause: NightShiftIgnoreSpec dataclass should be paired with load_ig…
  - effect: Unit and property tests can pass completely while scanner an…
- Pathspec library requires patterns to use forward slashes (POSIX-style paths) even on Windows; convert backslashes if needed before matching. _(spec: 106_nightshift_ignore, confidence: 0.90)_
  - effect: Default exclusions (hardcoded patterns) should not be overri…
- When testing file permissions (chmod), tests must skip on POSIX root or non-POSIX systems where permission semantics don't work as expected. _(spec: 106_nightshift_ignore, confidence: 0.90)_
  - effect: File I/O tests should use `tmp_path` fixture and always clea…
- Property-based testing with Hypothesis requires suppressing health checks like `HealthCheck.too_slow` for tests with file I/O and temporary directories. _(spec: 106_nightshift_ignore, confidence: 0.90)_
  - effect: File I/O tests should use `tmp_path` fixture and always clea…
- Property-based tests with pytest.mark.given must use suppress_health_check=[HealthCheck.function_scoped_fixture] when accessing tmp_path fixture to avoid hypothesis health check warnings. _(spec: 107_additional_language_analyzers, confidence: 0.90)_
- Fact IDs from multiple sources (DuckDB, causal traversal, entity lookup) are cast to strings and stored in ScoredFact dataclass; content strings are coerced to empty string if None. _(spec: 104_adaptive_retrieval, confidence: 0.90)_
- DuckDB 1.5.1 does not allow a read_only=True connection and a write connection to the same file in the same OS process simultaneously; they must be opened sequentially or in separate processes. _(spec: 105_db_plan_state, confidence: 0.90)_
  - effect: When persisting a plan to DuckDB, clear existing plan data (…
- Migration version numbers in task documentation may become outdated when earlier migrations are added after planning; use the actual implemented version number in code comments and track it separately from spec documentation. _(spec: 105_db_plan_state, confidence: 0.90)_
  - effect: Use IF NOT EXISTS clauses in both CREATE TABLE and ALTER TAB…
- When using Hypothesis with pytest fixtures (like tmp_path), suppress the function_scoped_fixture health check to avoid warnings during property-based testing. _(spec: 103_agent_conversation_trace, confidence: 0.90)_
  - effect: Implement defensive error handling in I/O operations by catc…
- Property-based tests with Hypothesis should use unique identifiers (UUIDs) per iteration to prevent file accumulation and path conflicts in tmp_path fixtures. _(spec: 105_db_plan_state, confidence: 0.90)_
  - cause: JSONL trace files should be flushed immediately after each w…
- Session outcome records with `error_message=None` must store SQL NULL in the database, not an empty string, to distinguish successful completions from failures. _(spec: 105_db_plan_state, confidence: 0.90)_
  - effect: Run totals accumulation must use UPDATE statements with += o…
- Floating-point cost accumulation in aggregates should use approximate equality checks (e.g., `abs(actual - expected) < 1e-4`) rather than exact equality to avoid precision errors. _(spec: 105_db_plan_state, confidence: 0.90)_
  - effect: Run totals accumulation must use UPDATE statements with += o…
- Hypothesis property tests with `st.text()` strategy may generate whitespace-only strings; use `.filter(lambda s: s.strip())` to avoid meaningless test cases. _(spec: 103_remove_hook_runner, confidence: 0.90)_
- Configuration generation helpers (bounds maps, descriptions) must be kept in sync with actual config classes; removing a config field requires removing corresponding entries from these maps. _(spec: 103_remove_hook_runner, confidence: 0.90)_
  - cause: ADR 02: Remove Fox Tools

# ADR 02: Remove Fox Tools

## Sta…
- When both bash_allowlist and bash_allowlist_extend are configured, bash_allowlist takes precedence and bash_allowlist_extend should be ignored with a warning logged. _(spec: 103_remove_hook_runner, confidence: 0.90)_
  - cause: Shell operators (pipes, semicolons, subshells, redirects) an…
- For autonomous code fixing, check for a dirty working tree before proceeding and require the developer to commit or stash uncommitted changes to avoid accidental data loss. _(spec: 103_remove_hook_runner, confidence: 0.90)_
  - cause: When simplifying code, inline single-use abstractions (delet…
  - effect: 01. Use Claude Exclusively for Coding Agents

# 01. Use Clau…
- Never remove error handling, logging, or 'why' comments during refactoring; these are load-bearing even if they appear cluttered. Only delete comments that restate what code does. _(spec: 103_remove_hook_runner, confidence: 0.90)_
  - effect: When simplifying code, inline single-use abstractions (delet…
- Property-based tests can reveal duplicate-content bugs that unit tests might miss; verify test data generation doesn't produce redundant inputs. _(spec: 111_rich_memory_rendering, confidence: 0.60)_
  - effect: Comprehensive verification should include design specificati…
- Topological sort initialization uses a deque with newly-ready facts inserted in score-descending order, but the queue is processed FIFO, so lower-scoring facts may be processed before higher-scoring ones added later. _(spec: 104_adaptive_retrieval, confidence: 0.60)_
- Migration version numbers may skip versions in documentation if a newer migration landed before the documented one was implemented; update comments to reflect actual migration versions applied. _(spec: 104_adaptive_retrieval, confidence: 0.60)_
  - cause: Use IF NOT EXISTS in migration CREATE TABLE and ALTER TABLE …

## Patterns

- Wiring verification requires tracing data flow through the call stack with exact line number citations to confirm return values propagate correctly between functions. _(spec: 110_hunt_dedup_and_ignore, confidence: 0.90)_
  - cause: All intentional stubs and early-exit returns should be expli…
- Fail-open behavior (returning empty results on connection/query failure) should be justified with requirement citations in code audits; `_query_false_positives()` returns empty lists per requirement 110-REQ-6.E1. _(spec: 110_hunt_dedup_and_ignore, confidence: 0.90)_
  - effect: All intentional stubs and early-exit returns should be expli…
- Early-exit guard clauses returning empty lists (e.g., `return []` when input is empty) are legitimate implementations, not stubs, and should be distinguished from unimplemented functionality. _(spec: 110_hunt_dedup_and_ignore, confidence: 0.90)_
  - effect: All intentional stubs and early-exit returns should be expli…
- Validation should catch circular dependencies and unresolvable cross-spec references at plan time, not at execution time, by checking that all spec names exist, group numbers are in range, and dependency graphs are acyclic. _(spec: 110_hunt_dedup_and_ignore, confidence: 0.90)_
  - cause: Excluded directories list for non-gitignore fallback scans s…
  - cause: Task group completion state should be tracked in a dedicated…
- File layout annotations in design.md should include [group N] tags to identify which task group produces each file, enabling scope guard pre-flight checks and TASK-004 validation for overlapping deliverables. _(spec: 110_hunt_dedup_and_ignore, confidence: 0.90)_
  - cause: Design documents should explicitly name concrete file paths …
  - cause: Task group numbering must be sequential with no gaps (1, 2, …
  - effect: Dependency relationships between specs should be documented …
- Dependency relationships between specs should be documented with specific types, functions, or interfaces rather than generic descriptions to catch contract mismatches during spec authoring rather than at runtime. _(spec: 110_hunt_dedup_and_ignore, confidence: 0.90)_
  - cause: File layout annotations in design.md should include [group N…
  - effect: Validation should catch circular dependencies and unresolvab…
- Spec artifacts should remain immutable during plan execution by mounting .specs/ read-only in sandbox mode and excluding it from agent writable paths in non-sandbox mode to prevent mid-execution contract violations. _(spec: 110_hunt_dedup_and_ignore, confidence: 0.90)_
  - effect: Validation should catch circular dependencies and unresolvab…
- Task group completion state should be tracked in a dedicated state file (plan.json) rather than in markdown checkboxes in tasks.md, as checkbox syntax in markdown is fragile and prone to corruption during agent edits and concurrent merges. _(spec: 110_hunt_dedup_and_ignore, confidence: 0.90)_
  - effect: Validation should catch circular dependencies and unresolvab…
- Excluded directories list for non-gitignore fallback scans should include virtual environment directories (.venv, venv, env, .env) and tox (.tox) to prevent scanning tool-generated artifacts. _(spec: 110_hunt_dedup_and_ignore, confidence: 0.90)_
  - effect: Validation should catch circular dependencies and unresolvab…
- Harness protocol should separate concerns between orchestrator and agent runtime through a clean abstraction layer while being honest that Claude Code is the primary implementation with specific capabilities others may lack. _(spec: 110_hunt_dedup_and_ignore, confidence: 0.90)_
  - cause: 01. Use Claude Exclusively for Coding Agents

# 01. Use Clau…
  - cause: ADR 02: Remove Fox Tools

# ADR 02: Remove Fox Tools

## Sta…
- Specs must remain immutable during execution to preserve the deterministic contract between human intent and machine execution; enforcement uses read-only mount in sandbox mode and system prompt instructions in no-sandbox mode. _(spec: 110_hunt_dedup_and_ignore, confidence: 0.90)_
  - cause: Task group completion state should be tracked in a dedicated…
- Virtual environment directories (.venv, venv, env, .env, .tox) should be excluded from non-gitignore fallback directory scans to avoid unnecessary processing of dependency files. _(spec: 110_hunt_dedup_and_ignore, confidence: 0.90)_
- The hunt dedup and ignore pipeline should follow a specific sequence: ingest_ignore_signals() pre-phase, then _query_false_positives() to read anti_pattern facts, consolidate findings, and finally filter_known_duplicates() and filter_ignored() with similarity_threshold and embedder. _(spec: 110_hunt_dedup_and_ignore, confidence: 0.90)_
  - cause: The NightShiftEngine initialization requires conn and embedd…
  - effect: Anti_pattern facts stored in DuckDB should be passed through…
- The NightShiftEngine initialization requires conn and embedder parameters to support the enhanced hunt scan pipeline, particularly for querying anti_pattern facts from DuckDB and filtering operations. _(spec: 110_hunt_dedup_and_ignore, confidence: 0.90)_
  - effect: The hunt dedup and ignore pipeline should follow a specific …
- Protocol interfaces should be updated before or alongside implementing their methods in concrete classes to catch missing implementations early via type checking. _(spec: 110_hunt_dedup_and_ignore, confidence: 0.90)_
  - cause: When adding a new method to a protocol interface (like Platf…
- Batch embedding calls can be optimized by concatenating multiple text lists (groups + issues) into a single embed_batch call rather than embedding separately, then slicing results by offset. _(spec: 111_rich_memory_rendering, confidence: 0.90)_
  - effect: The filter_ignored function uses strict greater-than (>) com…
- Fail-open behavior (returning all unfiltered groups on error) should be implemented at multiple points: missing embedder, missing ignore issues, and exception handling during platform or embedding calls. _(spec: 111_rich_memory_rendering, confidence: 0.90)_
  - cause: The filter_ignored function uses strict greater-than (>) com…
- Embedding similarity-based filtering for suppressing FindingGroups should include full fail-open error handling to ensure that errors during embedding computation or similarity calculation don't accidentally suppress valid groups. _(spec: 110_hunt_dedup_and_ignore, confidence: 0.90)_
  - cause: When implementing embedding-based filtering with a fail-open…
  - effect: When fetching issues for filtering purposes (e.g., af:ignore…
- When modifying function signatures for backward compatibility, ensure optional parameters default to None to maintain compatibility with existing callers across multiple modules (engine.py, barrier.py, test_confidence.py). _(spec: 111_rich_memory_rendering, confidence: 0.90)_
  - effect: Comprehensive verification should include design specificati…
- Cross-spec entry point verification ensures top-level CLI commands actually invoke spec functions in production code through the full call chain, not just in test code. _(spec: 109_ai_fix_pipeline_wiring, confidence: 0.90)_
- A stub/dead-code audit requires searching touched files for patterns like `return []`, `return None` on non-Optional returns, `pass`, `# TODO`, `# stub`, and `NotImplementedError`; each hit must be justified or removed. _(spec: 109_ai_fix_pipeline_wiring, confidence: 0.90)_
- Return value propagation must be verified by grepping for all callers of data-returning functions and confirming none discard results; use grep to find callers and verify extend/assignment operations. _(spec: 109_ai_fix_pipeline_wiring, confidence: 0.90)_
- End-to-end execution path verification requires reading actual calling code rather than assuming function calls; trace every caller of each function in the chain to confirm wiring is live in production. _(spec: 109_ai_fix_pipeline_wiring, confidence: 0.90)_
- Use batch embedding with cosine similarity comparison for near-duplicate detection rather than comparing individual items, improving efficiency and consistency. _(spec: 110_hunt_dedup_and_ignore, confidence: 0.90)_
  - cause: Implement fingerprint-based short-circuit evaluation before …
  - effect: Accept similarity_threshold and embedder as configurable par…
- Implement fingerprint-based short-circuit evaluation before expensive similarity computations to optimize deduplication performance. _(spec: 110_hunt_dedup_and_ignore, confidence: 0.90)_
  - cause: When fetching issues for deduplication, use state='all' to r…
  - effect: Use batch embedding with cosine similarity comparison for ne…
- When fetching issues for deduplication, use state='all' to retrieve both open and closed issues, ensuring comprehensive duplicate detection across the full issue history. _(spec: 110_hunt_dedup_and_ignore, confidence: 0.90)_
  - effect: Implement fingerprint-based short-circuit evaluation before …
- When wiring AI-based fixes into linting pipelines, ensure per-spec error isolation, maintain ordering guarantees, and perform single-pass re-validation to prevent cascading failures and maintain data consistency. _(spec: 109_ai_fix_pipeline_wiring, confidence: 0.90)_
  - cause: ---
Title: 03. Adopt iterative two-phase fix architecture
Da…
- Use Pydantic field_validator to enforce numeric bounds on configuration fields like similarity_threshold, clamping values to valid ranges [0.0, 1.0]. _(spec: 110_hunt_dedup_and_ignore, confidence: 0.90)_
  - cause: Threshold-based configuration fields should have sensible de…
  - effect: Threshold-based configuration fields should have sensible de…
- Use `assume()` in Hypothesis tests to filter out invalid edge cases (e.g., zero vectors) that would make the test meaningless or break mathematical assumptions. _(spec: 110_hunt_dedup_and_ignore, confidence: 0.90)_
  - cause: Test all three similarity comparison modes: identical vector…
- Test all three similarity comparison modes: identical vectors (1.0), orthogonal vectors (0.0), and opposite vectors (-1.0) to verify cosine similarity implementation correctness. _(spec: 110_hunt_dedup_and_ignore, confidence: 0.90)_
  - effect: Use `assume()` in Hypothesis tests to filter out invalid edg…
- Platform mock classes should support both successful and failing scenarios via constructor parameters (e.g., `raises=True`) to test graceful degradation. _(spec: 110_hunt_dedup_and_ignore, confidence: 0.90)_
  - effect: Edge case tests should have dedicated test classes with clea…
- Mock embedders should implement an `embed_batch_call_count` attribute to verify that embedding operations are called the expected number of times (or not called at all for short-circuit optimization). _(spec: 110_hunt_dedup_and_ignore, confidence: 0.90)_
  - effect: Edge case tests should have dedicated test classes with clea…
- Helper functions like `_make_finding()` and `_make_group()` should accept **overrides to allow flexible test data creation with sensible defaults. _(spec: 110_hunt_dedup_and_ignore, confidence: 0.90)_
  - effect: Hypothesis property tests should be marked with `@pytest.mar…
- Use hypothesis strategies st.floats() with allow_nan=False and allow_infinity=False to avoid invalid test data in property tests, and use assume() to filter generated values. _(spec: 111_rich_memory_rendering, confidence: 0.90)_
  - effect: Property-based tests using Hypothesis should be marked with …
- Mock platforms should accept parameters to configure return values (hunt_issues, ignore_issues) and optionally raise exceptions, with a list to track all method calls for verification. _(spec: 111_rich_memory_rendering, confidence: 0.90)_
  - effect: Test docstrings should include the test case identifier (e.g…
- When testing async functions with asyncio, create a new event loop with asyncio.new_event_loop(), run the coroutine with loop.run_until_complete(), and ensure cleanup in a finally block. _(spec: 111_rich_memory_rendering, confidence: 0.90)_
  - effect: Test docstrings should include the test case identifier (e.g…
- Property-based tests using Hypothesis should be marked with @pytest.mark.property and organized in classes separate from unit tests, with @settings(max_examples=N) to control iteration count. _(spec: 111_rich_memory_rendering, confidence: 0.90)_
  - cause: Use hypothesis strategies st.floats() with allow_nan=False a…
- Mock embedders for testing should implement an embed_batch() method that returns a list of vectors (or None values), and track call count to verify whether the embedder was invoked. _(spec: 111_rich_memory_rendering, confidence: 0.90)_
  - effect: Edge case tests should be organized in separate test classes…
- Test files should use helper functions like _make_finding() and _make_group() to create test objects with sensible defaults, allowing focused override of only the fields relevant to each test. _(spec: 111_rich_memory_rendering, confidence: 0.90)_
  - effect: Test docstrings should include the test case identifier (e.g…
- Use tmp_path and tmp_path_factory fixtures to create temporary spec directories for testing file I/O and spec parsing without polluting the real filesystem. _(spec: 109_ai_fix_pipeline_wiring, confidence: 0.90)_
  - effect: Conditional file creation and mocking: only mock generators …
- Isolate errors in multi-spec pipelines by catching exceptions per spec and continuing with others, then returning results from non-failing specs to avoid losing valid fixes. _(spec: 109_ai_fix_pipeline_wiring, confidence: 0.90)_
  - cause: Enforce ordering guarantees in async pipelines by completing…
  - cause: Invoke the AI fix pipeline at most once per run, even if re-…
- Property-based testing with Hypothesis can generate random inputs (rule types, batch sizes, finding counts) to verify that code behavior holds across a wide range of parameter combinations. _(spec: 109_ai_fix_pipeline_wiring, confidence: 0.90)_
  - effect: Batch API calls using ceiling division (ceil(n / batch_size)…
- Use side_effect in mocks to simulate sequential behavior or state changes, and call_order lists to verify execution sequence in async test flows. _(spec: 109_ai_fix_pipeline_wiring, confidence: 0.90)_
  - cause: Use AsyncMock from unittest.mock for mocking async functions…
- Enforce ordering guarantees in async pipelines by completing all criteria rewrites (fix_ai_criteria) before starting test spec generation (fix_ai_test_spec_entries) to ensure file state consistency. _(spec: 109_ai_fix_pipeline_wiring, confidence: 0.90)_
  - cause: Organize AI fix dispatch logic by rule type: route vague-cri…
  - effect: Isolate errors in multi-spec pipelines by catching exception…
- Organize AI fix dispatch logic by rule type: route vague-criterion and implementation-leak findings to rewrite_criteria(), and untraced-requirement findings to generate_test_spec_entries(). _(spec: 109_ai_fix_pipeline_wiring, confidence: 0.90)_
  - cause: Batch API calls using ceiling division (ceil(n / batch_size)…
  - effect: Enforce ordering guarantees in async pipelines by completing…
- Extract criterion/requirement IDs from Finding message text using pattern matching (e.g., '[01-REQ-1.1]') to build a findings_map for dispatching fixes to the correct handlers. _(spec: 109_ai_fix_pipeline_wiring, confidence: 0.90)_
- Batch API calls using ceiling division (ceil(n / batch_size) = -(-n // batch_size)) to respect rate limits or API constraints, splitting findings into multiple calls when they exceed a batch limit constant. _(spec: 109_ai_fix_pipeline_wiring, confidence: 0.90)_
  - cause: Property-based testing with Hypothesis can generate random i…
  - effect: Organize AI fix dispatch logic by rule type: route vague-cri…
- Use AsyncMock from unittest.mock for mocking async functions in pytest, and mark async tests with @pytest.mark.asyncio to properly execute them. _(spec: 109_ai_fix_pipeline_wiring, confidence: 0.90)_
  - effect: Use side_effect in mocks to simulate sequential behavior or …
- Spec test files should comprehensively cover the feature surface (21 tests across 5 files in this case) to catch multiple failure modes: missing modules, missing parameters, and missing configuration fields. _(spec: 110_hunt_dedup_and_ignore, confidence: 0.90)_
  - cause: When creating spec test files for unimplemented features, ex…
- When creating spec test files for unimplemented features, expect ImportError, TypeError, and AttributeError failures initially, but verify that existing tests (4784+ in this case) continue to pass to ensure no regressions. _(spec: 110_hunt_dedup_and_ignore, confidence: 0.90)_
  - effect: Spec test files should comprehensively cover the feature sur…
- Critical domain findings (like case-sensitive entity type values: 'file' not 'File') should be explicitly incorporated into test data to prevent future regressions. _(spec: 111_rich_memory_rendering, confidence: 0.90)_
  - cause: When writing tests ahead of implementation (TDD), ensure all…
  - effect: When implementing a feature with multiple test categories (u…
- When writing tests ahead of implementation (TDD), ensure all test cases are syntactically valid and lint-clean before implementation begins, even if they fail with ImportError or AssertionError. _(spec: 111_rich_memory_rendering, confidence: 0.90)_
  - effect: Critical domain findings (like case-sensitive entity type va…
- Integration tests for spec linting should include cases for archived dependencies to ensure the validator correctly handles specs that have been moved to archive directories. _(spec: 109_ai_fix_pipeline_wiring, confidence: 0.90)_
  - cause: Spec dependency documentation should reference the group num…
- When building a dependency validation map, scan both active and archived spec directories to avoid flagging references to fully-implemented archived specs as broken dependencies. _(spec: 109_ai_fix_pipeline_wiring, confidence: 0.90)_
  - effect: When iterating through multiple directories with overlapping…
- When merging specs from multiple directories into a lookup map, prioritize active specs over archived copies with the same name to prevent archived versions from overwriting current implementations. _(spec: 110_hunt_dedup_and_ignore, confidence: 0.90)_
  - cause: When building a spec dependency validator, scan both active …
  - effect: Integration tests for spec linting should verify that depend…
- When building a spec dependency validator, scan both active and archived spec directories to avoid flagging references to fully-implemented archived specs as broken dependencies. _(spec: 110_hunt_dedup_and_ignore, confidence: 0.90)_
  - effect: When merging specs from multiple directories into a lookup m…
- When scanning multiple directories for specs, prioritize active specs over archived copies with the same name by checking if an entry already exists in the known_specs map before overwriting it. _(spec: 111_rich_memory_rendering, confidence: 0.90)_
  - cause: When building a specs dependency map, scan both active and a…
  - effect: Avoid overwriting active spec metadata when also scanning ar…
- When building a specs dependency map, scan both active and archived spec directories to avoid flagging references to fully-implemented archived specs as broken dependencies. _(spec: 111_rich_memory_rendering, confidence: 0.90)_
  - effect: When scanning multiple directories for specs, prioritize act…
  - effect: Dependency validators must include archived specs in their r…
- Property-based tests should include generated test data for newly added fields and update field mapping dictionaries to ensure coverage of override logic. _(spec: 99_archetype_profiles, confidence: 0.90)_
  - cause: When adding a field to a dataclass that appears in multiple …
- Use conditional ternary expressions in dataclass.replace() to implement inheritance semantics: override with mode value if not None, otherwise fall back to base entry value. _(spec: 99_archetype_profiles, confidence: 0.90)_
  - cause: When adding a field to a dataclass that appears in multiple …
  - effect: Unit tests for partial overrides should explicitly verify bo…
- When adding a field to a dataclass that appears in multiple related classes, ensure the field is added to all relevant classes and that override logic in resolver functions is updated to handle the new field. _(spec: 99_archetype_profiles, confidence: 0.90)_
  - effect: Use conditional ternary expressions in dataclass.replace() t…
  - effect: Property-based tests should include generated test data for …
- A comprehensive verification approach for wiring includes checking all execution paths, return value propagation, absence of unjustified stubs, importability without circular imports, and passing all quality gates (unit tests, property tests, smoke tests, and linter). _(spec: 95_entity_graph, confidence: 0.90)_
- When updating property-based tests for dataclass field changes, update both the strategy builders and the field mapping dictionaries to maintain consistency across test cases. _(spec: 98_reviewer_consolidation, confidence: 0.90)_
  - cause: When adding a field to a dataclass used in `dataclasses.repl…
  - effect: Fields that map 1:1 between dataclasses (e.g., ModeConfig.te…
- When adding a field to a dataclass used in `dataclasses.replace()`, ensure the field is explicitly handled in the replace call with proper None-checking logic to allow inheritance from base values. _(spec: 98_reviewer_consolidation, confidence: 0.90)_
  - cause: Use `field(default_factory=list)` for mutable default values…
  - effect: When updating property-based tests for dataclass field chang…
- Property-based tests should include the newly added field in both the strategy generators and the field mapping constants to ensure comprehensive coverage of override behavior. _(spec: 93_fix_branch_push, confidence: 0.90)_
  - cause: When adding a field with a mutable default (list), use `fiel…
- When a dataclass field has a default_factory (like empty list), use the ternary pattern `(override if override is not None else base)` in resolve functions to allow mode configs to explicitly override to non-None values. _(spec: 93_fix_branch_push, confidence: 0.90)_
  - cause: Use field-level mapping documentation (like _MODE_TO_ENTRY_F…
  - effect: When adding a field with a mutable default (list), use `fiel…
- Use field-level mapping documentation (like _MODE_TO_ENTRY_FIELD_MAP) to clarify how optional override fields in one config class map to fields in another, especially when names differ (e.g., 'model_tier' → 'default_model_tier'). _(spec: 93_fix_branch_push, confidence: 0.90)_
  - cause: When restoring a previously removed field to satisfy spec re…
  - cause: Reserved fields that are not yet consumed by production code…
  - effect: When a dataclass field has a default_factory (like empty lis…
- When restoring a previously removed field to satisfy spec requirements, document the deviation and resolution in an errata file to explain why it was removed and why it's being restored. _(spec: 93_fix_branch_push, confidence: 0.90)_
  - effect: Use field-level mapping documentation (like _MODE_TO_ENTRY_F…
- When adding fields to dataclasses used in property-based tests, update both the strategy definition and the field mapping dictionary to ensure comprehensive test coverage. _(spec: 97_archetype_modes, confidence: 0.90)_
  - cause: Use a ternary expression pattern `(mode_cfg.field if mode_cf…
  - cause: Reserved/future fields in dataclasses should be explicitly d…
  - effect: Field default factories (e.g., `field(default_factory=list)`…
- Use a ternary expression pattern `(mode_cfg.field if mode_cfg.field is not None else entry.field)` in dataclass.replace() to implement optional field overrides that fall back to base values when not specified. _(spec: 97_archetype_modes, confidence: 0.90)_
  - cause: When adding a new optional override field to ModeConfig, ens…
  - effect: When adding fields to dataclasses used in property-based tes…
- When adding a new optional override field to ModeConfig, ensure it is also added to ArchetypeEntry with appropriate defaults and included in the resolve_effective_config() mapping to avoid spec violations. _(spec: 97_archetype_modes, confidence: 0.90)_
  - effect: Use a ternary expression pattern `(mode_cfg.field if mode_cf…
- Use field(default_factory=list) for mutable default values in dataclasses to avoid shared state across instances, while optional override fields can use None as default. _(spec: 92_transient_audit_reports, confidence: 0.90)_
  - effect: When adding a new field to a dataclass that participates in …
- Property-based tests should include new fields in strategy definitions and in the field mapping dict to ensure comprehensive coverage of override behavior. _(spec: 92_transient_audit_reports, confidence: 0.90)_
  - cause: When adding a new field to a dataclass that participates in …
- Use conditional expressions (ternary operator) with None checks when resolving configuration overrides: `override_value if override_value is not None else base_value` allows None to mean 'inherit'. _(spec: 92_transient_audit_reports, confidence: 0.90)_
  - effect: When adding a new field to a dataclass that participates in …
- When adding a new field to a dataclass that participates in mode overrides, update resolve_effective_config() to include the field in the override mapping, and document the field mapping in comments. _(spec: 92_transient_audit_reports, confidence: 0.90)_
  - cause: Use conditional expressions (ternary operator) with None che…
  - cause: Use field(default_factory=list) for mutable default values i…
  - effect: Property-based tests should include new fields in strategy d…
  - effect: When restoring removed fields due to spec violations, create…
- When a feature branch is at the same commit as develop and all tests pass with all subtasks completed, verify that the work was already merged in a prior session before making new changes. _(spec: 95_entity_graph, confidence: 0.90)_
  - effect: Use commit hashes and test suite status (unit tests, smoke t…
- Test both full override and partial override scenarios separately to ensure fields correctly inherit base values when not explicitly overridden. _(spec: 96_knowledge_consolidation, confidence: 0.90)_
  - cause: Maintain a mapping dictionary between source and target fiel…
- Maintain a mapping dictionary between source and target field names when fields have different names across configuration layers (e.g., ModeConfig to ArchetypeEntry). _(spec: 96_knowledge_consolidation, confidence: 0.90)_
  - cause: When a field can be overridden at multiple levels (base entr…
  - effect: Test both full override and partial override scenarios separ…
- When a field can be overridden at multiple levels (base entry and mode-specific config), use `is not None` checks to distinguish between explicit None (inherit) and user-provided values in the resolution logic. _(spec: 96_knowledge_consolidation, confidence: 0.90)_
  - cause: Use `field(default_factory=list)` for list fields in datacla…
  - effect: Maintain a mapping dictionary between source and target fiel…
- When restoring a previously removed field to satisfy spec requirements, document the deviation and resolution in an errata file to provide context for future maintainers. _(spec: 96_knowledge_consolidation, confidence: 0.90)_
  - effect: Use `field(default_factory=list)` for list fields in datacla…
- When modifying data model fields, update both unit tests and property tests to cover the new field. _(spec: 97_archetype_modes, confidence: 0.90)_
  - cause: When adding override fields to configuration classes, ensure…
- When adding override fields to configuration classes, ensure resolve_effective_config() is updated to handle the new field's override logic. _(spec: 97_archetype_modes, confidence: 0.90)_
  - cause: The ModeConfig.templates field was previously removed but is…
  - effect: When modifying data model fields, update both unit tests and…
- When audit report paths change, existing tests must be updated to reference the new paths. _(spec: 92_transient_audit_reports, confidence: 0.90)_
  - cause: Audit reports for transient_audit_reports are written to `.a…
  - cause: PASS verdicts delete existing audit reports rather than over…
- The memory.md file serves as a structured knowledge base organized by category (Gotchas, Patterns, Decisions, Conventions, Anti-patterns, Fragile Areas) with confidence scores and spec references for future task matching. _(spec: 97_archetype_modes, confidence: 0.90)_
- Different reviewer modes (pre-review, fix-review, drift-review, audit-review) have distinct responsibilities and output formats; each mode focuses on specific aspects (spec quality, implementation correctness, code-spec alignment, test coverage). _(spec: 97_archetype_modes, confidence: 0.90)_
- Intermediate analysis text during reviewer tool calls is discarded; only the final message is parsed for structured results, so analysis can be verbose between tool invocations. _(spec: 97_archetype_modes, confidence: 0.90)_
- The pre-review mode examines specifications for contradictions, ambiguities, missing requirements, and correctness risks using severity levels, focusing on completeness, consistency, feasibility, testability, edge cases, and security. _(spec: 95_entity_graph, confidence: 0.90)_
- The fix-review mode verifies Coder implementation against acceptance criteria by running tests and producing per-criterion PASS/FAIL verdicts with evidence, resulting in an overall FAIL if any individual verdict is FAIL. _(spec: 95_entity_graph, confidence: 0.90)_
- The drift-review mode compares spec design assumptions against actual codebase to identify deviations, using severity levels (critical, major, minor, observation) and focusing on file existence, signatures, and API contracts before behavioral assumptions. _(spec: 95_entity_graph, confidence: 0.90)_
- The audit-review mode validates test coverage by checking each TS entry has corresponding test functions with verdicts (PASS, WEAK, MISSING, MISALIGNED) and produces an overall FAIL if any MISSING, MISALIGNED, or 2+ WEAK entries exist. _(spec: 95_entity_graph, confidence: 0.90)_
- Fix-review verifies implementation against acceptance criteria by running tests and code inspection, producing per-criterion PASS/FAIL verdicts with evidence. _(spec: 93_fix_branch_push, confidence: 0.90)_
- Audit-review validates test coverage by tracing each test spec (TS) entry to concrete passing tests, assessing coverage, assertion strength, precondition fidelity, edge cases, and independence. _(spec: 93_fix_branch_push, confidence: 0.90)_
  - cause: Pre-review audits focus on specification quality before codi…
- Pre-review audits focus on specification quality before coding (contradictions, ambiguities, completeness, testability), while drift-review compares actual code against spec assumptions. _(spec: 93_fix_branch_push, confidence: 0.90)_
  - effect: Audit-review validates test coverage by tracing each test sp…
- Intermediate messages between tool calls may contain analysis text, but only the final message is parsed; everything before it is discarded by the system. _(spec: 93_fix_branch_push, confidence: 0.90)_
  - cause: Final reviewer output must be a single bare JSON object with…
- Reviewer profiles are templated workflow policies for different review modes (pre-review, fix-review, drift-review, audit-review), each with specific focus areas, constraints, and JSON output format requirements. _(spec: 93_fix_branch_push, confidence: 0.90)_
  - cause: ---
Title: 05. Adopt MAGMA multi-graph memory architecture
D…
- Pre-review and drift-review modes use severity levels (critical, major, minor, observation) for findings, while fix-review uses PASS/FAIL verdicts and audit-review uses PASS/WEAK/MISSING/MISALIGNED verdicts per entry. _(spec: 92_transient_audit_reports, confidence: 0.90)_
- Audit-review mode validates test coverage against test_spec.md contracts by checking that each TS entry has a concrete passing test, with verdicts (PASS/WEAK/MISSING/MISALIGNED) based on coverage, assertion strength, preconditions, edge cases, and independence. _(spec: 92_transient_audit_reports, confidence: 0.90)_
- Drift-review mode compares spec design assumptions against actual codebase to identify divergence, checking file existence, class/function signatures, and API contracts using read-only tools (no tests or builds). _(spec: 92_transient_audit_reports, confidence: 0.90)_
- Fix-review mode verifies that coder implementations satisfy acceptance criteria by running the test suite (make check, uv run pytest) and producing PASS/FAIL verdicts per criterion with supporting evidence. _(spec: 92_transient_audit_reports, confidence: 0.90)_
- Pre-review mode examines specifications for contradictions, ambiguities, missing requirements, and correctness risks before coding begins, using only read-only tools (ls, cat, git, wc, head, tail) without grep or find. _(spec: 92_transient_audit_reports, confidence: 0.90)_
- Reviewer profiles operate in four distinct modes (pre-review, fix-review, drift-review, audit-review), each with different responsibilities, constraints, and output formats. The mode determines which parts of the codebase are analyzed and what verdicts are produced. _(spec: 92_transient_audit_reports, confidence: 0.90)_
- Pre-review must examine specifications for contradictions, ambiguities, missing requirements, and correctness risks across completeness, consistency, feasibility, testability, edge cases, and security dimensions before coding begins. _(spec: 97_archetype_modes, confidence: 0.90)_
  - cause: Reviewer roles operate in distinct modes (pre-review, fix-re…
- Reviewer constraints vary by mode: pre-review and drift-review are read-only (no grep/find for pre-review), fix-review can run tests and linters, audit-review can run pytest with collect-only and test filtering, and drift-review cannot run any build commands. _(spec: 97_archetype_modes, confidence: 0.90)_
  - effect: Audit-review verdicts per test spec entry are PASS (adequate…
- Reviewer roles operate in distinct modes (pre-review, fix-review, drift-review, audit-review) with different responsibilities: pre-review validates specs before coding, fix-review verifies implementation against acceptance criteria, drift-review checks spec-to-code alignment, and audit-review validates test coverage against test specs. _(spec: 97_archetype_modes, confidence: 0.90)_
  - effect: Drift-review findings use severity levels (critical, major, …
  - effect: Pre-review must examine specifications for contradictions, a…
- Memory.md documents structured learnings extracted from coding sessions, including gotchas, patterns, decisions, conventions, anti-patterns, and fragile areas with confidence levels and keywords for future task matching. _(spec: 93_fix_branch_push, confidence: 0.90)_
- Audit-review mode validates test coverage against test_spec.md contracts, checking coverage existence, assertion strength, precondition fidelity, edge case rigor, and independence, producing PASS/WEAK/MISSING/MISALIGNED verdicts per TS entry. _(spec: 93_fix_branch_push, confidence: 0.90)_
- Drift-review mode compares spec design assumptions against actual codebase using read-only file inspection (ls, cat, git, grep, find) to identify deviations in files, classes, functions, signatures, APIs, and behavior. _(spec: 93_fix_branch_push, confidence: 0.90)_
- Fix-review mode verifies Coder implementations against acceptance criteria by running test suites and code inspection, producing per-criterion PASS/FAIL verdicts based on evidence, not vague observations. _(spec: 93_fix_branch_push, confidence: 0.90)_
- Pre-review mode validates specs for completeness, consistency, feasibility, testability, edge cases, and security before coding begins, using read-only operations and reporting severity levels (critical, major, minor, observation). _(spec: 93_fix_branch_push, confidence: 0.90)_
- Reviewer profiles define four distinct modes (pre-review, fix-review, drift-review, audit-review), each with specific focus areas, constraints, and structured JSON output requirements that must be strictly followed to avoid expensive retry loops. _(spec: 93_fix_branch_push, confidence: 0.90)_
- Pre-review mode identifies specification defects (contradictions, ambiguities, missing requirements, edge cases, security risks) before coding begins; severity levels distinguish between critical blockers and informational observations. _(spec: 92_transient_audit_reports, confidence: 0.90)_
- Fix-review mode verifies that implementation satisfies acceptance criteria by running `make check` and producing per-criterion PASS/FAIL verdicts with evidence; overall verdict must be FAIL if any individual verdict is FAIL. _(spec: 92_transient_audit_reports, confidence: 0.90)_
- Drift-review mode validates specification assumptions against actual codebase; findings must reference both spec locations and artifact paths, with severity levels (critical/major/minor/observation) to prioritize investigation. _(spec: 92_transient_audit_reports, confidence: 0.90)_
- Audit-review mode requires structured per-entry verdicts (PASS/WEAK/MISSING/MISALIGNED) with references to specific TS entries and test function paths; overall verdict is FAIL if any MISSING or MISALIGNED entries exist or 2+ WEAK entries are present. _(spec: 92_transient_audit_reports, confidence: 0.90)_
- Reviewer pre-review mode examines specifications before coding to identify contradictions, ambiguities, missing requirements, and feasibility risks using severity levels and evidence-based findings. _(spec: 95_entity_graph, confidence: 0.90)_
- Reviewer fix-review mode verifies implementation against acceptance criteria by running test suite and checking root cause fixes, error handling, and edge case coverage, then produces per-criterion verdicts (PASS/FAIL). _(spec: 95_entity_graph, confidence: 0.90)_
- Reviewer drift-review mode identifies discrepancies between spec design assumptions and actual codebase using severity levels (critical, major, minor, observation), scanning file existence, signatures, and behavioral contracts before deep dives. _(spec: 95_entity_graph, confidence: 0.90)_
- Reviewer audit-review mode validates test coverage by comparing TS entries from test_spec.md against concrete passing tests, using verdicts: PASS (adequate), WEAK (insufficient), MISSING (absent), MISALIGNED (wrong scenario). _(spec: 95_entity_graph, confidence: 0.90)_
- Audit-review mode validates test coverage by matching TS entries from test_spec.md to concrete passing tests, assigning verdicts (PASS, WEAK, MISSING, MISALIGNED) and failing overall if any entry is MISSING, MISALIGNED, or 2+ WEAK. _(spec: 97_archetype_modes, confidence: 0.90)_
- Pre-review mode focuses on spec quality (completeness, consistency, feasibility, testability, edge cases, security) before coding; drift-review compares spec assumptions against actual codebase; fix-review validates acceptance criteria against implementation. _(spec: 97_archetype_modes, confidence: 0.90)_
- A stub/dead-code audit must check for common indicators: `return []`, `return None` on non-Optional returns, `pass` in non-abstract methods, `# TODO`, `# stub`, `override point`, and `NotImplementedError`; each hit requires justification or replacement. _(spec: 107_additional_language_analyzers, confidence: 0.90)_
  - cause: End-to-end wiring verification requires tracing every execut…
  - cause: Return value propagation should be verified by grepping for …
- Return value propagation should be verified by grepping for all callers of a function and confirming none discard the return value; verify the value is actually consumed downstream in the call chain. _(spec: 107_additional_language_analyzers, confidence: 0.90)_
  - cause: End-to-end wiring verification requires tracing every execut…
  - effect: A stub/dead-code audit must check for common indicators: `re…
- When wrapping optional imports in try/except blocks, use try/except ImportError for the import itself and try/except Exception for downstream operations like parser probes, with appropriate logging for missing dependencies. _(spec: 107_additional_language_analyzers, confidence: 0.90)_
- End-to-end wiring verification requires tracing every execution path from design documentation through actual code, confirming each function in the chain is live (not a stub) and that no function returns empty collections or None on non-Optional returns. _(spec: 107_additional_language_analyzers, confidence: 0.90)_
  - effect: Return value propagation should be verified by grepping for …
  - effect: A stub/dead-code audit must check for common indicators: `re…
- Language analyzer implementations should include an extension uniqueness property test to ensure no duplicate extensions are registered. _(spec: 107_additional_language_analyzers, confidence: 0.90)_
  - cause: When completing a language analyzer feature, validate by run…
  - effect: Smoke tests (integration tests) are a useful checkpoint for …
- When completing a language analyzer feature, validate by running the full test suite, verifying new analyzers appear in the default registry, and confirming integration smoke tests pass. _(spec: 107_additional_language_analyzers, confidence: 0.90)_
  - effect: Language analyzer implementations should include an extensio…
  - effect: Smoke tests (integration tests) are a useful checkpoint for …
- Language analyzers should implement the LanguageAnalyzer protocol and be registered in registry.py to integrate with the analysis framework. _(spec: 107_additional_language_analyzers, confidence: 0.90)_
- Language analyzers need to extract both entities (FILE, MODULE, CLASS, FUNCTION) and edges (CONTAINS, IMPORTS, EXTENDS) along with a module map to provide complete code knowledge. _(spec: 107_additional_language_analyzers, confidence: 0.90)_
  - cause: tree-sitter-kotlin 1.x has a grammar bug that causes false-p…
  - effect: Language analyzers should be registered in a central registr…
- Executing all integration smoke tests with real components (no stub bypass) is essential to validate that wiring works in production scenarios. _(spec: 106_nightshift_ignore, confidence: 0.90)_
  - cause: ---
Title: 05. Adopt MAGMA multi-graph memory architecture
D…
- Return value propagation must be verified end-to-end: confirm the caller receives the return, uses it, and does not discard it. _(spec: 106_nightshift_ignore, confidence: 0.90)_
  - cause: ---
Title: 05. Adopt MAGMA multi-graph memory architecture
D…
- Verify cross-spec integration by tracing actual production code calls—read the calling code, grep for callers, and confirm return values are used rather than discarded. _(spec: 106_nightshift_ignore, confidence: 0.90)_
  - cause: ---
Title: 05. Adopt MAGMA multi-graph memory architecture
D…
- When auditing for stubs and dead code, distinguish between intentional graceful fallbacks (e.g., `return []` for file read errors) and actual stubs; document the rationale for each intentional one. _(spec: 106_nightshift_ignore, confidence: 0.90)_
  - cause: ---
Title: 05. Adopt MAGMA multi-graph memory architecture
D…
- Language analyzer registration should include graceful ImportError handling to allow optional language support without breaking the default registry. _(spec: 107_additional_language_analyzers, confidence: 0.90)_
  - effect: Language analyzers should implement the LanguageAnalyzer pro…
- Language analyzers should implement the LanguageAnalyzer protocol with entity extraction (FILE, MODULE, FUNCTION) and edge creation (CONTAINS, IMPORTS) to support the knowledge graph system. _(spec: 107_additional_language_analyzers, confidence: 0.90)_
  - cause: Language analyzer registration should include graceful Impor…
  - effect: Elixir's nested defmodule support requires special handling …
  - effect: Tree-sitter language parsers should be specified with versio…
- When adding new initialization logic to a codebase with both fresh and re-init paths, the new logic must be wired into both code paths to ensure consistent behavior across different initialization scenarios. _(spec: 106_nightshift_ignore, confidence: 0.90)_
- When integrating new modules with existing scanners, wrap the integration call in try/except to enable graceful degradation if the module fails, as specified in requirement documents. _(spec: 106_nightshift_ignore, confidence: 0.90)_
  - effect: The nightshift.ignore module provides load_ignore_spec and f…
- Use frozen dataclasses with the `replace()` function from dataclasses to create modified copies while preserving immutability, rather than mutating and reassigning fields. _(spec: 108_issue_session_summary, confidence: 0.90)_
  - cause: When filtering collections with a removal predicate, preserv…
  - effect: Initialize new optional fields in dataclasses with sensible …
- Gracefully degrade ignore spec loading by returning a defaults-only spec on any error (missing files, encoding errors, invalid patterns). Never raise exceptions during ignore file loading. _(spec: 108_issue_session_summary, confidence: 0.90)_
  - effect: When using pathspec for gitignore-style matching, maintain t…
- When using pathspec for gitignore-style matching, maintain two separate PathSpec objects: one for defaults-only and one for combined patterns. Check the defaults spec first to prevent user negation patterns from overriding critical exclusions. _(spec: 108_issue_session_summary, confidence: 0.90)_
  - cause: Gracefully degrade ignore spec loading by returning a defaul…
  - effect: Apply both .gitignore and .night-shift patterns additively i…
- NightShiftIgnoreSpec dataclass should be paired with load_ignore_spec() and filter_findings() functions to create a complete ignore specification system. _(spec: 106_nightshift_ignore, confidence: 0.90)_
  - cause: The pathspec library (version >=0.12) is required for implem…
  - effect: Project initialization must include an _ensure_nightshift_ig…
- Run both unit tests and property tests when adding new language analyzers to verify correctness and catch regressions. _(spec: 107_additional_language_analyzers, confidence: 0.90)_
  - cause: Language analyzer implementations should include both entity…
- Language analyzer implementations should include both entity/edge extraction and module mapping functionality to provide complete code analysis. _(spec: 107_additional_language_analyzers, confidence: 0.90)_
  - cause: When adding a new language analyzer, register it in the defa…
  - effect: Run both unit tests and property tests when adding new langu…
- When adding a new language analyzer, register it in the default registry after implementation to enable it for use. _(spec: 107_additional_language_analyzers, confidence: 0.90)_
  - cause: External language parsing dependencies (like tree-sitter-c-s…
  - effect: Language analyzer implementations should include both entity…
- Guard optional feature calls with None checks (e.g., platform != None) in finally blocks to ensure cleanup logic only runs when the feature is available. _(spec: 108_issue_session_summary, confidence: 0.90)_
  - cause: Use a factory function that returns None instead of calling …
  - effect: Track feature usage state with boolean flags (e.g., _issue_s…
- Use a factory function that returns None instead of calling sys.exit() for optional dependencies, allowing graceful degradation when a platform is unavailable. _(spec: 108_issue_session_summary, confidence: 0.90)_
  - effect: Guard optional feature calls with None checks (e.g., platfor…
- File I/O tests should use `tmp_path` fixture and always clean up in a try-finally block when manually manipulating permissions or other system state. _(spec: 106_nightshift_ignore, confidence: 0.90)_
  - cause: Property-based testing with Hypothesis requires suppressing …
  - cause: When testing file permissions (chmod), tests must skip on PO…
- For operations that may fail silently (e.g., file creation), verify logging occurs at the expected level rather than relying on exceptions. _(spec: 106_nightshift_ignore, confidence: 0.90)_
  - effect: When testing that a system continues to work despite an erro…
- When testing that a system continues to work despite an error in one component (e.g., ignore loading), patch the failing component and verify results are still produced. _(spec: 106_nightshift_ignore, confidence: 0.90)_
  - cause: Smoke tests should verify the entire pipeline works (e.g., i…
  - cause: For operations that may fail silently (e.g., file creation),…
- Integration tests should verify real end-to-end behavior by wiring together real implementations and only mocking external category detectors, not core filtering/loading functions. _(spec: 106_nightshift_ignore, confidence: 0.90)_
  - effect: Smoke tests should verify the entire pipeline works (e.g., i…
- Use helper functions to construct complex test objects (e.g., `_make_finding()`) with sensible defaults to reduce boilerplate and improve readability across many test cases. _(spec: 106_nightshift_ignore, confidence: 0.90)_
  - effect: Test structures benefit from having distinct test classes fo…
- Test structures benefit from having distinct test classes for different scenarios (unit, property, integration, smoke) with clear separation of concerns and naming conventions. _(spec: 106_nightshift_ignore, confidence: 0.90)_
  - cause: Use helper functions to construct complex test objects (e.g.…
- For filtering operations that remove items, verify the monotonicity property: output length <= input length, and all output items were in the input. _(spec: 106_nightshift_ignore, confidence: 0.90)_
  - cause: When a finding's affected_files list becomes empty after fil…
- Idempotency should be verified in tests by calling the operation multiple times and confirming state does not change after the first call. _(spec: 106_nightshift_ignore, confidence: 0.90)_
  - effect: Test that default values are documented and verified (e.g., …
- Graceful degradation pattern: when loading a configuration file fails (unreadable, corrupt), return sensible defaults rather than raising an exception. _(spec: 106_nightshift_ignore, confidence: 0.90)_
- External imports (e.g., 'using System' in C#, external dependencies in other languages) should be silently skipped and not produce IMPORTS edges when the target is not in the module map. _(spec: 107_additional_language_analyzers, confidence: 0.90)_
- All LanguageAnalyzer implementations must have non-empty language_name and file_extensions properties, and make_parser() must return a configured parser instance. _(spec: 107_additional_language_analyzers, confidence: 0.90)_
  - effect: Entity extraction must produce FILE entities with the filena…
- Language-specific keyword sets must be maintained as frozensets to filter out reserved words when generating test identifiers, preventing syntax errors in generated source code. _(spec: 107_additional_language_analyzers, confidence: 0.90)_
  - effect: Integration smoke tests should verify that analyze_codebase …
- When implementing spec tests before implementation, verify test files are syntactically valid and pass linting (ruff) before relying on expected ModuleNotFoundError failures to confirm tests are properly structured. _(spec: 106_nightshift_ignore, confidence: 0.90)_
  - effect: Organize failing tests across multiple test file types (unit…
- Async functions that iterate over collections and perform I/O operations should handle failures gracefully to prevent one failure from blocking remaining items. _(spec: 108_issue_session_summary, confidence: 0.90)_
  - cause: Pure functions like parse_source_url() should be separated f…
  - effect: Use dataclasses for structured data containers like SourceIs…
- Pure functions like parse_source_url() should be separated from side-effect functions like async post_issue_summaries() to maintain testability and clarity of intent. _(spec: 108_issue_session_summary, confidence: 0.90)_
  - cause: When executing git commands like 'git rev-parse develop', pr…
  - effect: Async functions that iterate over collections and perform I/…
- When executing git commands like 'git rev-parse develop', provide a fallback value (e.g., 'unknown') for cases where the command fails or the ref doesn't exist. _(spec: 108_issue_session_summary, confidence: 0.90)_
  - effect: Pure functions like parse_source_url() should be separated f…
- When writing tests before implementation (TDD), it is expected and correct for new tests to fail with ModuleNotFoundError; verify that pre-existing tests continue to pass to ensure no regressions. _(spec: 107_additional_language_analyzers, confidence: 0.90)_
  - cause: Use a naming convention like TS-107-1, TS-107-E9 for unit te…
- When implementing support for multiple programming languages (C#, Elixir, Kotlin, Dart), organize tests into three layers: unit tests for language-specific behavior, property tests for cross-language invariants, and integration smoke tests for end-to-end validation. _(spec: 107_additional_language_analyzers, confidence: 0.90)_
  - effect: Use a naming convention like TS-107-1, TS-107-E9 for unit te…
- When writing tests for functions that don't exist yet, existing test suites should remain unaffected; this session confirmed no regressions across 4475 existing tests when adding 21 new failing tests. _(spec: 108_issue_session_summary, confidence: 0.90)_
  - cause: Writing comprehensive spec tests before implementation is a …
- Writing comprehensive spec tests before implementation is a valid approach; 21 failing tests were created for multiple functions (parse_source_url, build_summary_comment, post_issue_summaries, etc.) with no regression in existing 4475 tests. _(spec: 108_issue_session_summary, confidence: 0.90)_
  - effect: When writing tests for functions that don't exist yet, exist…
- Version numbers must be synchronized across multiple files: __init__.py, pyproject.toml, and uv.lock to maintain consistency across the project. _(spec: 108_issue_session_summary, confidence: 0.90)_
  - effect: When updating version numbers, ensure all references in docu…
- Cross-spec entry points must be verified as being called from production code (not just tests) to ensure refactored components are actually used in real execution paths and integrated correctly. _(spec: 104_adaptive_retrieval, confidence: 0.90)_
  - cause: Dead-code audits should systematically search for anti-patte…
- Dead-code audits should systematically search for anti-patterns including `return []`, `return None` on non-Optional returns, `pass` in non-abstract methods, TODO comments, stubs, and NotImplementedError; each finding must be justified or removed. _(spec: 104_adaptive_retrieval, confidence: 0.90)_
  - cause: End-to-end wiring verification requires tracing every execut…
  - effect: Cross-spec entry points must be verified as being called fro…
- End-to-end wiring verification requires tracing every execution path from design documentation through actual code, confirming every function in the chain is live (not a stub) and return values propagate correctly between callers. _(spec: 104_adaptive_retrieval, confidence: 0.90)_
  - cause: A comprehensive verification group for major refactoring sho…
  - cause: When migrating from file-based to DB-based persistence, reta…
  - effect: Dead-code audits should systematically search for anti-patte…
- When migrating from file-based to DB-based persistence, retain backward-compatible APIs for 25+ existing dependent test files rather than forcing a full simultaneous migration to avoid large-scale regressions. _(spec: 104_adaptive_retrieval, confidence: 0.90)_
  - effect: End-to-end wiring verification requires tracing every execut…
- A comprehensive verification group for major refactoring should include: smoke tests, legacy removal tests, stub audits, execution path traceability, cross-spec entry point verification, full test suite passes, and linting checks. _(spec: 104_adaptive_retrieval, confidence: 0.90)_
  - cause: When removing memory.jsonl infrastructure, update .gitignore…
  - effect: End-to-end wiring verification requires tracing every execut…
- When removing memory.jsonl infrastructure, update .gitignore to remove the negation rule (!.agent-fox/memory.jsonl) alongside deleting JSONL export functions to fully decommission the legacy storage layer. _(spec: 104_adaptive_retrieval, confidence: 0.90)_
  - effect: A comprehensive verification group for major refactoring sho…
- When refactoring knowledge retrieval, consolidate multiple single-signal retrieval methods (keyword, vector, causal) into a unified multi-signal retriever with RRF fusion rather than juggling separate calls. _(spec: 104_adaptive_retrieval, confidence: 0.90)_
  - cause: Pre-computed fact caches become stale and expensive to maint…
- Use try-except with graceful fallback (DB first, file second) when adding new persistence layers to avoid breaking existing workflows during incremental migrations. _(spec: 104_adaptive_retrieval, confidence: 0.90)_
  - cause: When migrating from file-based to DB-based persistence, reta…
  - effect: Document breaking changes and backward-compatibility decisio…
- When migrating from file-based to DB-based persistence, retain the old API for backward compatibility with 25+ existing test files rather than forcing a full migration. _(spec: 104_adaptive_retrieval, confidence: 0.90)_
  - effect: Use try-except with graceful fallback (DB first, file second…
- Run-level aggregations like total tokens and cost should be updated incrementally as sessions complete rather than computed in batch at the end to support early termination and partial results. _(spec: 105_db_plan_state, confidence: 0.90)_
  - cause: Per-node status should be persisted to the database on each …
- Database operations in result handlers should be wrapped in try-except blocks with debug logging to prevent transient DB failures from crashing the main execution flow. _(spec: 105_db_plan_state, confidence: 0.90)_
  - cause: All cross-spec entry points must be verified to receive Duck…
- When wrapping functions that handle missing resources, return None with a clear docstring explaining the graceful fallback behavior rather than raising exceptions. _(spec: 105_db_plan_state, confidence: 0.90)_
- Database operations in critical paths should be wrapped in try-except blocks that log but don't crash, allowing the system to continue with graceful degradation when persistence operations fail. _(spec: 105_db_plan_state, confidence: 0.90)_
  - effect: When persisting node status per-transition to a database, re…
- Stub and dead-code audit must justify every instance of `return []`, `return None` on non-Optional returns, `pass` in non-abstract methods, and TODO comments; stubs should only remain if they have valid semantic justification (e.g., empty list for no predecessors). _(spec: 105_db_plan_state, confidence: 0.90)_
  - cause: Cross-spec entry point verification requires checking all ca…
- Cross-spec entry point verification requires checking all callers of critical functions and ensuring they pass the correct type (e.g., DuckDB connection object, not file path) to maintain consistency across the system. _(spec: 105_db_plan_state, confidence: 0.90)_
  - effect: Stub and dead-code audit must justify every instance of `ret…
- Graceful fallback pattern: when a critical resource (like db_path) is missing, return None with a documented reason rather than raising an exception, allowing callers to handle the absence gracefully. _(spec: 105_db_plan_state, confidence: 0.90)_
- Use try-except blocks with `logger.debug(exc_info=True)` when attempting optional DB operations (like loading from DB), allowing silent fallback to file-based operations without exposing internal errors to users. _(spec: 105_db_plan_state, confidence: 0.90)_
  - cause: Implement graceful fallback patterns: try DB-based operation…
- When removing global path constants from `core/paths.py`, define local path constants in each module that uses them as `Path(".agent-fox/plan.json")` rather than importing, to decouple modules from the paths layer. _(spec: 105_db_plan_state, confidence: 0.90)_
- Implement graceful fallback patterns: try DB-based operations first (when connection available), then fall back to file-based operations silently; this keeps legacy code paths working while gradually migrating to new storage layers. _(spec: 105_db_plan_state, confidence: 0.90)_
  - cause: ---
Title: 05. Adopt MAGMA multi-graph memory architecture
D…
  - effect: Use try-except blocks with `logger.debug(exc_info=True)` whe…
- Use parenthesized context managers syntax `with (...):` instead of nested `with` statements for better readability when multiple context managers are used. _(spec: 104_adaptive_retrieval, confidence: 0.90)_
- Context assembly guarantees output length ≤ token_budget using a two-phase approach: first try full detail for all facts, then apply salience-based trimming if over budget, finally hard-truncate result string. _(spec: 103_agent_conversation_trace, confidence: 0.90)_
- The RRF fusion formula is score(fact) = sum(weight_i / (k + rank_i)) where rank is 1-based per signal; facts appearing in multiple signals accumulate contributions from each signal independently. _(spec: 103_agent_conversation_trace, confidence: 0.90)_
- The RetrievalConfig is nested under KnowledgeConfig with default_factory=RetrievalConfig, allowing configuration via [knowledge.retrieval] TOML table while maintaining backward compatibility. _(spec: 103_agent_conversation_trace, confidence: 0.90)_
- The entity and causal signals reuse existing traversal functions (find_related_facts, traverse_causal_chain) and convert their results into ScoredFact objects, treating confidence as the score for entity-linked facts and proximity (1 / (|depth| + 1)) for causal facts. _(spec: 103_agent_conversation_trace, confidence: 0.90)_
- The keyword signal scores facts by keyword_match_count plus a recency bonus normalized by the total age range of relevant facts, ensuring both keyword relevance and temporal freshness. _(spec: 103_agent_conversation_trace, confidence: 0.90)_
- All signal functions wrap database queries and traversals in try/except blocks to gracefully degrade if a single signal fails, preventing one failure from invalidating the entire retrieval. _(spec: 103_agent_conversation_trace, confidence: 0.90)_
- Salience-based token budgeting assigns facts to tiers (high: top 20%, medium: 40%, low: 40%) and renders high-tier facts at full detail, medium at one-line summary, and omits low-tier facts if budget is exceeded. _(spec: 103_agent_conversation_trace, confidence: 0.90)_
- The context assembly process uses topological sorting to enforce causal ordering (causes before effects) using Kahn's algorithm, with ties broken by descending score. _(spec: 103_agent_conversation_trace, confidence: 0.90)_
- Intent profiles are derived from a lookup table mapping (archetype, node_status) tuples to weight multipliers; unknown archetypes fall back to a balanced default profile with all weights = 1.0. _(spec: 103_agent_conversation_trace, confidence: 0.90)_
- The adaptive retriever uses weighted Reciprocal Rank Fusion (RRF) to fuse results from four independent signals (keyword, vector, entity graph, causal chain), with per-signal weights derived from an intent profile based on task archetype and node status. _(spec: 103_agent_conversation_trace, confidence: 0.90)_
- AdaptiveRetriever.retrieve() derives intent profile, runs all four signals in sequence with try/except for vector signal specifically, fuses results via RRF, and returns a RetrievalResult object with context string and observability metadata. _(spec: 104_adaptive_retrieval, confidence: 0.90)_
  - cause: Reciprocal Rank Fusion (RRF) deduplicates facts by ID and ac…
  - cause: Intent profiles are derived from (archetype, node_status) tu…
- RetrievalConfig is a nested dataclass under KnowledgeConfig with defaults (rrf_k=60, max_facts=50, token_budget=30000) and uses extra='ignore' to allow forward compatibility. _(spec: 104_adaptive_retrieval, confidence: 0.90)_
- Causal signal finds same-spec facts, traverses their causal chains, and orders results by proximity (ascending absolute depth), allowing both forward (effect) and backward (cause) traversal. _(spec: 104_adaptive_retrieval, confidence: 0.90)_
  - effect: AdaptiveRetriever.retrieve() derives intent profile, runs al…
- Entity signal calls find_related_facts for each touched file with BFS traversal up to max_depth, deduplicating results by fact ID across all input files. _(spec: 104_adaptive_retrieval, confidence: 0.90)_
  - effect: AdaptiveRetriever.retrieve() derives intent profile, runs al…
- Vector signal uses cosine similarity between embedded task description and stored embeddings, requiring an embedder with an embedding_dimensions attribute and falling back to config defaults. _(spec: 104_adaptive_retrieval, confidence: 0.90)_
  - effect: AdaptiveRetriever.retrieve() derives intent profile, runs al…
- Keyword signal scoring combines keyword match count with a recency bonus normalized by the age range of all candidate facts, allowing recent facts to score higher even with fewer keyword matches. _(spec: 104_adaptive_retrieval, confidence: 0.90)_
  - effect: AdaptiveRetriever.retrieve() derives intent profile, runs al…
- The four signal functions (keyword, vector, entity, causal) wrap database queries and graph traversals in try/except blocks for graceful degradation; failures log warnings and return empty lists rather than propagating exceptions. _(spec: 104_adaptive_retrieval, confidence: 0.90)_
  - effect: AdaptiveRetriever.retrieve() derives intent profile, runs al…
- Token budgeting in context assembly first attempts full detail rendering; if over budget, applies salience-based trimming (high/medium/low); final output is truncated to guarantee len(output) <= token_budget. _(spec: 104_adaptive_retrieval, confidence: 0.90)_
  - cause: Reciprocal Rank Fusion (RRF) deduplicates facts by ID and ac…
- Salience-based context assembly assigns tiers based on score percentile: top 20% high-tier (full detail), next 40% medium-tier (one-line summary), bottom 40% low-tier; high/medium tiers render fully, low tier is omitted if over budget. _(spec: 104_adaptive_retrieval, confidence: 0.90)_
  - cause: Topological sorting of facts respects causal precedence (cau…
  - cause: Reciprocal Rank Fusion (RRF) deduplicates facts by ID and ac…
- Topological sorting of facts respects causal precedence (causes before effects) with ties broken by score; remaining unsorted facts (cycles) are appended at the end. _(spec: 104_adaptive_retrieval, confidence: 0.90)_
  - effect: Salience-based context assembly assigns tiers based on score…
- Intent profiles are derived from (archetype, node_status) tuples and map to per-signal weight multipliers; unknown archetypes fall back to a balanced default profile with all weights = 1.0. _(spec: 104_adaptive_retrieval, confidence: 0.90)_
  - effect: AdaptiveRetriever.retrieve() derives intent profile, runs al…
- Reciprocal Rank Fusion (RRF) deduplicates facts by ID and accumulates weighted contributions from each signal using the formula: score(fact) = sum(weight_i / (k + rank_i)) where k is a smoothing constant (default 60). _(spec: 104_adaptive_retrieval, confidence: 0.90)_
  - effect: Token budgeting in context assembly first attempts full deta…
  - effect: Salience-based context assembly assigns tiers based on score…
- Integration smoke tests should validate real component behavior end-to-end, covering the full pipeline from user command through infrastructure setup, query execution, and trace event emission to file write. _(spec: 103_agent_conversation_trace, confidence: 0.90)_
  - cause: ---
Title: 03. Adopt iterative two-phase fix architecture
Da…
- Cross-spec entry points (functions called from other specifications) must be verified to exist in production code at both definition and call sites to ensure inter-module contracts are honored. _(spec: 103_agent_conversation_trace, confidence: 0.90)_
  - cause: 01. Use Claude Exclusively for Coding Agents

# 01. Use Clau…
- A comprehensive audit for dead code and stubs should search touched files for patterns like `return []`, `return None` on non-Optional returns, `pass` in non-abstract methods, TODO comments, stub markers, and NotImplementedError—each finding must be justified or removed. _(spec: 103_agent_conversation_trace, confidence: 0.90)_
  - cause: 01. Use Claude Exclusively for Coding Agents

# 01. Use Clau…
- End-to-end wiring verification should trace execution paths from design documentation through actual code, confirming every function in the chain is live (not a stub) and return values propagate correctly between callers. _(spec: 103_agent_conversation_trace, confidence: 0.90)_
  - cause: ---
Title: 03. Adopt iterative two-phase fix architecture
Da…
- Use `TYPE_CHECKING` import guard to avoid circular imports and runtime overhead when importing DuckDB types only for type hints. _(spec: 105_db_plan_state, confidence: 0.90)_
  - effect: When wrapping a public-facing function that may fail (like g…
- Use `hasattr(value, 'isoformat')` to safely convert datetime objects from DuckDB query results to ISO format strings, since DuckDB may return native datetime objects. _(spec: 105_db_plan_state, confidence: 0.90)_
  - effect: Store NULL in the database (not empty string) for error_mess…
- Topological order must be preserved when round-tripping a TaskGraph through DuckDB by storing sort_position and loading nodes ordered by that field. _(spec: 105_db_plan_state, confidence: 0.90)_
  - effect: Public `save_plan()` and `load_plan()` functions should disp…
- When persisting a plan to DuckDB, clear existing plan data (DELETE from tables) before inserting in a single transaction to ensure idempotency. _(spec: 105_db_plan_state, confidence: 0.90)_
  - cause: DuckDB 1.5.1 does not allow a read_only=True connection and …
  - effect: Public `save_plan()` and `load_plan()` functions should disp…
- Public `save_plan()` and `load_plan()` functions should dispatch on argument type (Path vs DuckDB connection) to support both file-based and DB-based persistence for backward compatibility. _(spec: 105_db_plan_state, confidence: 0.90)_
  - cause: Plan content hash should exclude mutable runtime fields (lik…
  - cause: When persisting a plan to DuckDB, clear existing plan data (…
- Plan content hash should exclude mutable runtime fields (like status) so that status transitions do not invalidate the hash; only structural fields (nodes without status, edges, order) are included. _(spec: 105_db_plan_state, confidence: 0.90)_
  - effect: Public `save_plan()` and `load_plan()` functions should disp…
- Emit trace events before the main execution loop (for session.init) and after each message/outcome (for assistant.message, tool.use, tool.error, session.result) to capture the complete execution flow. _(spec: 103_agent_conversation_trace, confidence: 0.90)_
- When replacing a sink implementation, verify removal by both checking module file existence and searching for import statements using grep-like patterns with appropriate prefixes to avoid false positives. _(spec: 103_agent_conversation_trace, confidence: 0.90)_
  - cause: When replacing old implementations, update all import statem…
  - cause: Delete old test files (test_jsonl_sink.py, test_jsonl_respon…
- Delete old test files (test_jsonl_sink.py, test_jsonl_response.py) that exclusively test deprecated implementations when replacing core modules. _(spec: 103_agent_conversation_trace, confidence: 0.90)_
  - cause: When replacing old implementations, update all import statem…
  - effect: When replacing a sink implementation, verify removal by both…
- Dispatcher methods should wrap all sink calls in try-except blocks and log warnings on failure to ensure one sink's error does not prevent other sinks from recording events. _(spec: 103_agent_conversation_trace, confidence: 0.90)_
  - cause: Use duck-typing with hasattr() in dispatcher methods to invo…
  - effect: Use duck-typing with hasattr() in dispatcher methods to invo…
- When replacing old implementations, update all import statements across the codebase including test files, property tests, integration tests, and smoke tests to avoid stale references. _(spec: 103_agent_conversation_trace, confidence: 0.90)_
  - effect: Delete old test files (test_jsonl_sink.py, test_jsonl_respon…
  - effect: When replacing a sink implementation, verify removal by both…
- Use duck-typing with hasattr() in dispatcher methods to invoke sink methods only if they exist, allowing different sink implementations to support different trace events without requiring a common interface. _(spec: 103_agent_conversation_trace, confidence: 0.90)_
  - cause: Dispatcher methods should wrap all sink calls in try-except …
  - effect: Dispatcher methods should wrap all sink calls in try-except …
- Maintain the base schema DDL in a separate constant that mirrors migration changes, so fresh database initialization gets the latest schema without replaying all migrations. _(spec: 104_adaptive_retrieval, confidence: 0.90)_
  - cause: Use IF NOT EXISTS in migration CREATE TABLE and ALTER TABLE …
  - effect: Update test assertions that check schema version counts or m…
- When creating new database tables in migrations, include timestamps with DEFAULT CURRENT_TIMESTAMP and audit columns (created_at, updated_at) for traceability and future querying. _(spec: 104_adaptive_retrieval, confidence: 0.90)_
- When extending tables with ALTER TABLE ADD COLUMN in migrations, check if the table exists first to gracefully handle cases where the table may not be present (e.g., in test fixtures with minimal schemas). _(spec: 104_adaptive_retrieval, confidence: 0.90)_
  - cause: Use IF NOT EXISTS in migration CREATE TABLE and ALTER TABLE …
- Use IF NOT EXISTS in migration CREATE TABLE and ALTER TABLE ADD COLUMN statements to ensure idempotency across different database initialization paths (fresh vs. migrated). _(spec: 104_adaptive_retrieval, confidence: 0.90)_
  - effect: When extending tables with ALTER TABLE ADD COLUMN in migrati…
  - effect: Migration version numbers may skip versions in documentation…
- Update both base schema DDL and migration functions when introducing new tables/columns, and keep test expectations in sync (e.g., EXPECTED_TABLES, migration count assertions). _(spec: 105_db_plan_state, confidence: 0.90)_
  - cause: When extending existing tables in migrations, query informat…
  - cause: Allow nullable/optional fields in dataclass records (using |…
  - effect: Consolidate scattered dataclass fields (like SessionRecord +…
- When extending existing tables in migrations, query information_schema to check table existence first, especially in test environments where fixture schemas may be minimal, to avoid migration failures. _(spec: 105_db_plan_state, confidence: 0.90)_
  - cause: Use IF NOT EXISTS clauses in both CREATE TABLE and ALTER TAB…
  - effect: Update both base schema DDL and migration functions when int…
- Use IF NOT EXISTS clauses in both CREATE TABLE and ALTER TABLE ADD COLUMN statements within migrations to ensure idempotency across fresh databases and existing ones with partially applied schemas. _(spec: 105_db_plan_state, confidence: 0.90)_
  - cause: Migration version numbers in task documentation may become o…
  - effect: When extending existing tables in migrations, query informat…
- For JSONL event logging, include a timestamp field as ISO format UTC datetime and always flush after each write to ensure durability in streaming scenarios. _(spec: 103_agent_conversation_trace, confidence: 0.90)_
  - cause: Use shallow copying and selective truncation for large strin…
  - effect: Implement lazy file opening (on first write) to handle direc…
- Use shallow copying and selective truncation for large string values in nested dictionaries to manage output size while preserving all keys and non-string values. _(spec: 103_agent_conversation_trace, confidence: 0.90)_
  - cause: Implement defensive error handling in I/O operations by catc…
  - effect: For JSONL event logging, include a timestamp field as ISO fo…
- Implement defensive error handling in I/O operations by catching exceptions and logging warnings rather than re-raising, allowing session execution to continue even if tracing fails. _(spec: 103_agent_conversation_trace, confidence: 0.90)_
  - cause: When using Hypothesis with pytest fixtures (like tmp_path), …
  - effect: Use shallow copying and selective truncation for large strin…
- Failing tests should be created first (red phase) before implementation; imports will initially raise ModuleNotFoundError until the retrieval module is created in group 2. _(spec: 104_adaptive_retrieval, confidence: 0.90)_
  - cause: Keyword signal scores facts matching the spec_name exactly, …
- Entity signal performs BFS traversal of entity_graph to find facts linked via fact_entities; touched_files are matched against entity_path in the entity_graph table. _(spec: 104_adaptive_retrieval, confidence: 0.90)_
  - cause: Vector signal requires an embedder with embedding_dimensions…
  - effect: Keyword signal scores facts matching the spec_name exactly, …
- Token budget compliance in context assembly should be enforced: output length must never exceed the configured token_budget in RetrievalConfig. _(spec: 104_adaptive_retrieval, confidence: 0.90)_
  - cause: Property tests should use Hypothesis @st.composite strategie…
  - effect: RetrievalResult must include signal_counts (dict of signal_n…
- Causal ordering must be enforced in context assembly: predecessors in fact_causes edges must appear before effects in the final output. _(spec: 104_adaptive_retrieval, confidence: 0.90)_
  - cause: Confidence thresholds in knowledge retrieval are used to fil…
  - effect: RRF (Reciprocal Rank Fusion) score formula is weight / (k + …
- Integration tests for retrieval pipelines should mock the embedder (return values) but NOT mock the signal functions, RRF fusion, or context assembly—these must be tested with real implementations. _(spec: 104_adaptive_retrieval, confidence: 0.90)_
  - cause: Test files should use in-memory DuckDB with schema from migr…
  - effect: Confidence thresholds in knowledge retrieval are used to fil…
- Test files should use in-memory DuckDB with schema from migrations (run_migrations) to ensure all tables including entity_graph, fact_entities, and fact_causes exist before tests run. _(spec: 104_adaptive_retrieval, confidence: 0.90)_
  - effect: Integration tests for retrieval pipelines should mock the em…
- Non-string values in dictionaries (integers, booleans, lists) passed through truncation functions should remain unchanged and not be converted or copied. _(spec: 105_db_plan_state, confidence: 0.90)_
  - cause: String truncation in tool_input should preserve the original…
- Write failures in logging/tracing code should be caught and logged as warnings rather than propagated, allowing the application to continue despite audit I/O issues. _(spec: 105_db_plan_state, confidence: 0.90)_
  - cause: When testing I/O failure scenarios with read-only directorie…
  - effect: Audit retention logic should preserve agent trace files (age…
- Audit retention logic should preserve agent trace files (agent_*.jsonl) even when pruning audit event files (audit_*.jsonl) during cleanup. _(spec: 105_db_plan_state, confidence: 0.90)_
  - cause: Write failures in logging/tracing code should be caught and …
  - effect: Test files with staged removal (group 1 → group 3) should in…
- When testing I/O failure scenarios with read-only directories, restore original permissions in a finally block to ensure test cleanup succeeds. _(spec: 105_db_plan_state, confidence: 0.90)_
  - effect: Write failures in logging/tracing code should be caught and …
- String truncation in tool_input should preserve the original string up to max_len characters, then append ' [truncated]' marker for tracking and debugging. _(spec: 105_db_plan_state, confidence: 0.90)_
  - cause: Large prompt fields (system_prompt, task_prompt) should be c…
  - effect: Non-string values in dictionaries (integers, booleans, lists…
- JSONL trace files should be flushed immediately after each write to ensure data is persisted and readable by concurrent processes or subsequent reads. _(spec: 105_db_plan_state, confidence: 0.90)_
  - cause: Test specs should be marked as passing (checked) only after …
  - effect: Property-based tests with Hypothesis should use unique ident…
- session.init, assistant.message, tool.use, tool.error, and session.result events all include a timestamp field that is auto-generated at event recording time. _(spec: 103_agent_conversation_trace, confidence: 0.90)_
  - effect: AgentTraceSink writes trace events to JSONL files at audit_d…
- Integration smoke tests use a MockBackend class that yields pre-configured canonical message sequences to test end-to-end wiring without requiring a real LLM backend. _(spec: 103_agent_conversation_trace, confidence: 0.90)_
  - effect: Property-based tests using Hypothesis generate run_id values…
- The audit retention mechanism (enforce_audit_retention) deletes old audit_*.jsonl files when max_runs is exceeded but preserves all agent_*.jsonl trace files regardless of retention policy. _(spec: 103_agent_conversation_trace, confidence: 0.90)_
- I/O failures during trace file writes (e.g., PermissionError) are caught and logged as warnings rather than propagating exceptions, allowing the agent session to continue. _(spec: 103_agent_conversation_trace, confidence: 0.90)_
  - cause: AgentTraceSink writes trace events to JSONL files at audit_d…
- AgentTraceSink automatically creates missing audit directories on first write using Path.mkdir(parents=True, exist_ok=True). _(spec: 103_agent_conversation_trace, confidence: 0.90)_
  - effect: AgentTraceSink writes trace events to JSONL files at audit_d…
- String values in tool_input dictionaries exceeding 10,000 characters are truncated to exactly max_len characters plus the literal suffix " [truncated]", while non-string values are preserved unchanged. _(spec: 103_agent_conversation_trace, confidence: 0.90)_
  - effect: AgentTraceSink writes trace events to JSONL files at audit_d…
- AgentTraceSink writes trace events to JSONL files at audit_dir/agent_{run_id}.jsonl with one JSON object per line, flushed immediately after each write. _(spec: 103_agent_conversation_trace, confidence: 0.90)_
  - cause: String values in tool_input dictionaries exceeding 10,000 ch…
  - cause: AgentTraceSink automatically creates missing audit directori…
  - effect: I/O failures during trace file writes (e.g., PermissionError…
  - effect: Trace file paths are constructed as audit_dir / f"agent_{run…
- Non-string values in tool_input dictionaries (integers, booleans, lists) should pass through unchanged during truncation, not be copied or modified. _(spec: 103_agent_conversation_trace, confidence: 0.90)_
  - cause: JsonlSink is being removed and replaced with AgentTraceSink;…
- AgentTraceSink must create the audit directory on first write if it doesn't exist, using parents=True to create intermediate directories. _(spec: 103_agent_conversation_trace, confidence: 0.90)_
  - cause: AgentTraceSink writes trace files to a canonical path format…
- The truncate_tool_input function truncates string values exceeding max_len (default 10,000 chars) with a ' [truncated]' marker, while preserving non-string values unchanged. _(spec: 103_agent_conversation_trace, confidence: 0.90)_
  - effect: Long prompts (system_prompt and task_prompt) in session.init…
- I/O errors during trace writes should be caught and logged as warnings rather than propagating exceptions to caller. _(spec: 103_agent_conversation_trace, confidence: 0.90)_
  - cause: AgentTraceSink writes trace files to a canonical path format…
- AgentTraceSink writes trace files to a canonical path format: audit_dir/agent_{run_id}.jsonl, with one JSON object per line (JSONL format). _(spec: 103_agent_conversation_trace, confidence: 0.90)_
  - effect: I/O errors during trace writes should be caught and logged a…
  - effect: AgentTraceSink must create the audit directory on first writ…
- Run totals accumulation must use UPDATE statements with += operations to ensure idempotency; multiple calls with the same deltas should accumulate correctly without double-counting. _(spec: 105_db_plan_state, confidence: 0.90)_
  - cause: Floating-point cost accumulation in aggregates should use ap…
  - cause: Session outcome records with `error_message=None` must store…
- Integration tests for concurrent access should use file-based DuckDB (not in-memory) and open separate connections with different read/write modes to verify isolation. _(spec: 105_db_plan_state, confidence: 0.90)_
  - cause: DuckDB supports read-only connections that can query tables …
  - cause: Test fixtures using in-memory DuckDB (`:memory:`) are suitab…
- Crash recovery requires a `reset_in_progress_nodes()` function that atomically transitions any 'in_progress' nodes back to 'pending' on orchestrator restart. _(spec: 105_db_plan_state, confidence: 0.90)_
  - effect: NodeStatus enum must include v3-specific values like 'cost_b…
- Property-based testing with Hypothesis requires careful strategy composition for generating valid acyclic TaskGraphs; sequential edges (only forward references) keep graphs simple while remaining valid. _(spec: 105_db_plan_state, confidence: 0.90)_
- Test fixtures using in-memory DuckDB (`:memory:`) are suitable for unit tests, while file-based DuckDB paths enable integration tests requiring persistence across connection cycles. _(spec: 105_db_plan_state, confidence: 0.90)_
  - cause: Schema DDL should be defined as a module-level constant stri…
  - effect: Integration tests for concurrent access should use file-base…
- DuckDB supports read-only connections that can query tables while a separate write connection holds locks, enabling concurrent status reads without blocking. _(spec: 105_db_plan_state, confidence: 0.90)_
  - effect: Integration tests for concurrent access should use file-base…
- When updating project versions, ensure all three version locations are updated together to prevent version mismatch issues. _(spec: 105_db_plan_state, confidence: 0.90)_
  - cause: Version numbers must be synchronized across multiple files: …
- When updating package versions, ensure all three locations (package metadata, source init, and dependency lock) are updated together to avoid version mismatches. _(spec: 105_db_plan_state, confidence: 0.90)_
  - cause: Version numbers must be synchronized across multiple files: …
- A comprehensive verification gate should include smoke tests, stub audits, live execution path traceability, production code integration, existing test suite validation, and linter checks before considering a major refactoring complete. _(spec: 103_remove_hook_runner, confidence: 0.90)_
- Cross-spec entry point verification must confirm that refactored functions are actually called from production code, not just test code, to ensure changes are live and integrated. _(spec: 103_remove_hook_runner, confidence: 0.90)_
- Dead-code audits should systematically search for anti-patterns like `return []`, `return None` on non-Optional returns, `pass` in non-abstract methods, TODO comments, stubs, and NotImplementedError, plus verify no orphaned imports remain. _(spec: 103_remove_hook_runner, confidence: 0.90)_
- Wiring verification requires tracing every execution path from design documentation end-to-end, including verifying imports resolve at runtime and return values propagate correctly through the call chain. _(spec: 103_remove_hook_runner, confidence: 0.90)_
- A comprehensive verification group should include smoke tests, stub audits, execution path traceability, production code entry point verification, full test suite passes, and linting checks to confirm a refactoring is complete and safe. _(spec: 103_remove_hook_runner, confidence: 0.90)_
- Cross-spec entry points must be verified as being called from production code, not just tests, to ensure the refactored components are actually used in real execution paths. _(spec: 103_remove_hook_runner, confidence: 0.90)_
- Dead-code audit should search for common stub patterns including `return []`, `return None` on non-Optional returns, `pass` in non-abstract methods, `# TODO`, `# stub`, and `NotImplementedError` to ensure no orphaned code remains after refactoring. _(spec: 103_remove_hook_runner, confidence: 0.90)_
- Wiring verification requires tracing every execution path from design documentation end-to-end, including verifying imports resolve at runtime and that return values propagate correctly through the call chain. _(spec: 103_remove_hook_runner, confidence: 0.90)_
- After removing optional parameters from a dataclass (like `hook_config` and `no_hooks`), update all instantiation sites in tests, including fixture creation and mock object construction. _(spec: 103_remove_hook_runner, confidence: 0.90)_
  - cause: When removing a feature (like hooks), systematically delete:…
  - cause: ADR 02: Remove Fox Tools

# ADR 02: Remove Fox Tools

## Sta…
- When removing a feature (like hooks), systematically delete: test files, fixtures in conftest, config sections in docs, CLI flags, and update expected config section lists in test constants. _(spec: 103_remove_hook_runner, confidence: 0.90)_
  - effect: After removing optional parameters from a dataclass (like `h…
- When refactoring tuple returns to dataclass returns, update all call sites to use attribute access (e.g., `result.config`) instead of tuple unpacking (e.g., `new_config, new_circuit = result`). _(spec: 103_remove_hook_runner, confidence: 0.90)_
  - cause: Use frozen dataclasses for immutable result objects returned…
  - cause: ADR 02: Remove Fox Tools

# ADR 02: Remove Fox Tools

## Sta…
- Use frozen dataclasses for immutable result objects returned from functions, then unpack by field name rather than tuple unpacking to improve readability and type safety. _(spec: 103_remove_hook_runner, confidence: 0.90)_
  - effect: When refactoring tuple returns to dataclass returns, update …
- When removing functionality, check for both explicit function calls and indirect references via mock patches in tests to ensure complete removal. _(spec: 103_remove_hook_runner, confidence: 0.90)_
  - cause: ADR 02: Remove Fox Tools

# ADR 02: Remove Fox Tools

## Sta…
- Test files must be updated alongside production code changes; remove test cases and mock patches for deleted features, and update test assertions that reference removed parameters. _(spec: 103_remove_hook_runner, confidence: 0.90)_
  - cause: ADR 02: Remove Fox Tools

# ADR 02: Remove Fox Tools

## Sta…
- When removing a feature, systematically delete configuration classes, CLI options, function parameters, and imports across all modules that reference them to avoid orphaned code. _(spec: 103_remove_hook_runner, confidence: 0.90)_
  - cause: ADR 02: Remove Fox Tools

# ADR 02: Remove Fox Tools

## Sta…
- The extract_command_name function should strip path prefixes using PurePosixPath.name to handle both direct commands and full paths (e.g., '/usr/bin/python3' → 'python3'). _(spec: 103_remove_hook_runner, confidence: 0.90)_
- When moving test files during a module relocation, ensure all import statements within the moved files are updated to reference the new module location. _(spec: 103_remove_hook_runner, confidence: 0.90)_
  - cause: When relocating a module, update the logger name to match th…
  - effect: Import statements in test files should be ordered with modul…
- Shell operators (pipes, semicolons, subshells, redirects) and dangerous argument tokens (e.g., find -exec) must be detected and rejected before checking the allowlist, as they can bypass command restrictions. _(spec: 103_remove_hook_runner, confidence: 0.90)_
  - effect: When both bash_allowlist and bash_allowlist_extend are confi…
- Use `dataclasses.fields()` to introspect a dataclass and verify it has exactly the expected field names, rather than manually checking attributes. _(spec: 103_remove_hook_runner, confidence: 0.90)_
- Use `Path.rglob('*.py')` to recursively find all Python files in a directory, then filter out `__pycache__` entries to avoid false positives in static analysis tests. _(spec: 103_remove_hook_runner, confidence: 0.90)_
  - effect: Define banned symbols or patterns as module-level constants …
- Test immutability of frozen dataclasses by verifying that mutation attempts raise `AttributeError` or `TypeError`, and catch both exceptions in the assertion. _(spec: 103_remove_hook_runner, confidence: 0.90)_
  - cause: Frozen dataclasses (`@dataclass(frozen=True)`) prevent mutat…
- Use `hypothesis` with `@given` and `@settings(max_examples=N)` decorators to run property-based tests that verify invariants hold across many generated inputs. _(spec: 103_remove_hook_runner, confidence: 0.90)_
- Test that a function returns `None` explicitly when a configuration parameter (like `config_path=None`) disables its primary functionality, rather than relying on implicit behavior. _(spec: 103_remove_hook_runner, confidence: 0.90)_
- Frozen dataclasses (`@dataclass(frozen=True)`) prevent mutation and should be used for immutable value objects like configuration results; access frozen dataclass fields by attribute name rather than tuple indexing. _(spec: 103_remove_hook_runner, confidence: 0.90)_
  - effect: Test immutability of frozen dataclasses by verifying that mu…
- Use `@pytest.mark.parametrize` with a list of test variants to avoid code duplication when testing multiple input cases with the same assertions. _(spec: 103_remove_hook_runner, confidence: 0.90)_
  - effect: Use `# noqa: PLC0415` comment on local imports inside test f…
- Use `@pytest.mark.parametrize` to test multiple TOML variants in a single test method, ensuring all edge cases (empty, partial, unknown keys) are covered. _(spec: 103_remove_hook_runner, confidence: 0.90)_
- When relocating a module to a new package path, write separate test classes for 'new path works' and 'old path fails' to ensure clean migration with no residual imports. _(spec: 103_remove_hook_runner, confidence: 0.90)_
  - cause: ADR 02: Remove Fox Tools

# ADR 02: Remove Fox Tools

## Sta…
- For dataclass migrations, explicitly verify frozen state with `__dataclass_params__.frozen`, field names with `dataclasses.fields()`, and immutability by catching `AttributeError` on mutation attempts. _(spec: 103_remove_hook_runner, confidence: 0.90)_
- Use `rglob()` to recursively scan production code for banned symbols and collect violations as a list of (path, symbol) tuples for clear error reporting. _(spec: 103_remove_hook_runner, confidence: 0.90)_
- When removing a module, verify backward compatibility by testing that TOML files with legacy sections (e.g., [hooks]) parse successfully without errors. _(spec: 103_remove_hook_runner, confidence: 0.90)_
  - cause: ADR 02: Remove Fox Tools

# ADR 02: Remove Fox Tools

## Sta…
- Use hypothesis property tests with `@given` and `@settings` decorators to verify behavioral invariants (e.g., return types are always (bool, str)) across a range of inputs. _(spec: 103_remove_hook_runner, confidence: 0.90)_
  - cause: 01. Use Claude Exclusively for Coding Agents

# 01. Use Clau…
- When writing spec-driven tests, organize them into multiple test files by concern (removal, relocation, reload result) and use Test Spec identifiers (TS-103-X) to trace requirements. _(spec: 103_remove_hook_runner, confidence: 0.90)_
  - cause: 01. Use Claude Exclusively for Coding Agents

# 01. Use Clau…
- When simplifying code, inline single-use abstractions (delete the interface, keep the implementation) and avoid introducing over-engineered patterns like Factory, Builder, or Visitor preemptively. _(spec: 103_remove_hook_runner, confidence: 0.90)_
  - cause: Never remove error handling, logging, or 'why' comments duri…
  - effect: For autonomous code fixing, check for a dirty working tree b…
- For code simplification, structure the refactoring plan into tiers (quick wins, structural changes, design-level refactoring) so users can apply them selectively and maintain working state incrementally. _(spec: 103_remove_hook_runner, confidence: 0.90)_
  - cause: When implementing autonomous fixes via gh CLI, always post a…
  - effect: Test code should never be refactored for DRYness; tests shou…
- When implementing autonomous fixes via gh CLI, always post an analysis comment to the issue before modifying code to create a transparent audit trail and allow clarification requests. _(spec: 103_remove_hook_runner, confidence: 0.90)_
  - effect: For code simplification, structure the refactoring plan into…
- When creating ADRs, the code is the single source of truth and should be deeply read and verified; documentation and comments frequently diverge from actual implementation. _(spec: 103_remove_hook_runner, confidence: 0.90)_
  - cause: Architecture Decision Records (ADRs) should be stored in a c…
  - effect: ADRs should document at least one real alternative with hone…
- Anti_pattern facts stored in DuckDB should be passed through _query_false_positives() and then to consolidate_findings() as part of the false positive filtering workflow. _(spec: 110_hunt_dedup_and_ignore, confidence: 0.60)_
  - cause: The hunt dedup and ignore pipeline should follow a specific …
  - effect: The CLI entry point (cli/nightshift.py) is responsible for c…
- Function parameters like false_positives in consolidate_findings() should be added to enhance existing functionality when new features require filtering or conditional behavior. _(spec: 110_hunt_dedup_and_ignore, confidence: 0.60)_
- When fetching issues for filtering purposes (e.g., af:ignore issues), use state='all' to capture all relevant records rather than relying on default state filters. _(spec: 110_hunt_dedup_and_ignore, confidence: 0.60)_
  - cause: Embedding similarity-based filtering for suppressing Finding…
- Implement both async and sync wrapper functions for async dispatch operations (_apply_ai_fixes_async for core logic, _apply_ai_fixes for synchronous wrapper) to maintain API flexibility. _(spec: 109_ai_fix_pipeline_wiring, confidence: 0.60)_
  - cause: ---
Title: 03. Adopt iterative two-phase fix architecture
Da…
- When implementing batched async operations, use separate constants for different batch types (e.g., _MAX_REWRITE_BATCH and _MAX_UNTRACED_BATCH) to allow independent tuning of batch sizes for different processing phases. _(spec: 109_ai_fix_pipeline_wiring, confidence: 0.60)_
  - cause: ---
Title: 03. Adopt iterative two-phase fix architecture
Da…
- When implementing rendering features with multiple test types (unit, property, integration), ensure all test categories pass together to catch regressions across different test methodologies. _(spec: 111_rich_memory_rendering, confidence: 0.60)_
  - cause: EntityType enum values use uppercase (e.g., EntityType.FILE)…
- Threshold-based configuration fields should have sensible defaults (e.g., 0.85 for similarity) and explicit validation ranges documented in the spec. _(spec: 110_hunt_dedup_and_ignore, confidence: 0.60)_
  - cause: Use Pydantic field_validator to enforce numeric bounds on co…
  - effect: Use Pydantic field_validator to enforce numeric bounds on co…
- Conditional file creation and mocking: only mock generators and validators when testing dispatch logic; do not mock fix_ai_criteria() or fix_ai_test_spec_entries() in integration tests to verify actual file modifications. _(spec: 109_ai_fix_pipeline_wiring, confidence: 0.60)_
  - cause: Use tmp_path and tmp_path_factory fixtures to create tempora…
- Unit tests for partial overrides should explicitly verify both the overridden field and inherited fields to catch regressions in inheritance behavior. _(spec: 99_archetype_profiles, confidence: 0.60)_
  - cause: Use conditional ternary expressions in dataclass.replace() t…
  - cause: When restoring or deferring a field implementation, document…
- Reserved fields for future functionality should be clearly marked in comments (e.g., '97-REQ-1.3 (reserved; profiles used in practice)') and have corresponding errata explaining why they exist but are unused. _(spec: 98_reviewer_consolidation, confidence: 0.60)_
- Smoke tests (integration tests) are a useful checkpoint for verifying multiple new language analyzers work correctly together before marking a feature complete. _(spec: 107_additional_language_analyzers, confidence: 0.60)_
  - cause: When completing a language analyzer feature, validate by run…
  - cause: Language analyzer implementations should include an extensio…
- Unit and property tests can pass completely while scanner and integration tests fail due to unimplemented downstream task groups, indicating interdependencies between spec task groups. _(spec: 106_nightshift_ignore, confidence: 0.60)_
  - cause: Project initialization must include an _ensure_nightshift_ig…
- Track feature usage state with boolean flags (e.g., _issue_summaries_posted) to prevent duplicate operations or ensure proper sequencing in optional workflows. _(spec: 108_issue_session_summary, confidence: 0.60)_
  - cause: Guard optional feature calls with None checks (e.g., platfor…
- Test that default values are documented and verified (e.g., InitResult.nightshift_ignore defaults to 'skipped' if not provided). _(spec: 106_nightshift_ignore, confidence: 0.60)_
  - cause: Idempotency should be verified in tests by calling the opera…
- Smoke tests should verify the entire pipeline works (e.g., init → load → filter) with minimal mocking, as a sanity check before detailed unit tests. _(spec: 106_nightshift_ignore, confidence: 0.60)_
  - cause: Integration tests should verify real end-to-end behavior by …
  - effect: When testing that a system continues to work despite an erro…
- Integration smoke tests should verify that analyze_codebase returns languages_analyzed including all supported languages and that entities are correctly inserted into the database for each language. _(spec: 107_additional_language_analyzers, confidence: 0.60)_
  - cause: Language-specific keyword sets must be maintained as frozens…
  - cause: Module map values must always be POSIX-style relative paths …
- Multiple namespaces in a single file (language-dependent) should produce separate MODULE entities, allowing files to contain code from multiple logical modules. _(spec: 107_additional_language_analyzers, confidence: 0.60)_
  - effect: Integration smoke tests should verify that analyze_codebase …
- When multiple specs have pre-existing test failures, clearly document which failures are unrelated to current work to avoid confusion during debugging and review. _(spec: 106_nightshift_ignore, confidence: 0.60)_
  - cause: Organize failing tests across multiple test file types (unit…
- The memory.md file serves as a knowledge base that accumulates learnings across multiple specifications; clearing it during major refactoring requires careful preservation of high-confidence patterns and gotchas that remain relevant. _(spec: 108_issue_session_summary, confidence: 0.60)_
  - cause: Task completion tracking in spec documents should mark items…
- Documentation should be updated alongside version bumps to reflect current version examples in memory/knowledge files. _(spec: 106_nightshift_ignore, confidence: 0.60)_
  - cause: Version numbers must be synchronized across multiple files: …
- When updating version numbers, ensure all references in documentation files (like memory.md) are also updated to reflect the new version number. _(spec: 108_issue_session_summary, confidence: 0.60)_
  - cause: Version numbers must be synchronized across multiple files: …
  - cause: Pre-release versions use the semantic versioning format 'X.Y…
- Import heavy dependencies (like UUID generation and state management functions) inside try-except blocks in result handlers to avoid module-level coupling and enable graceful degradation. _(spec: 105_db_plan_state, confidence: 0.60)_
- When computing plan hashes, use graph-based computation (`compute_plan_hash(graph)`) when the graph is already loaded in memory, falling back to file-based computation only if the graph is unavailable. _(spec: 105_db_plan_state, confidence: 0.60)_
  - cause: ---
Title: 05. Adopt MAGMA multi-graph memory architecture
D…
- When wrapping a public-facing function that may fail (like generate_status), return None on missing resources and document the graceful fallback behavior for callers. _(spec: 105_db_plan_state, confidence: 0.60)_
  - cause: Use `TYPE_CHECKING` import guard to avoid circular imports a…
- Use StrEnum for status fields to ensure type safety and prevent invalid string values at the type level, particularly for extensible enumerations like NodeStatus. _(spec: 105_db_plan_state, confidence: 0.60)_
- Consolidate scattered dataclass fields (like SessionRecord + ExecutionState.session_history) into unified record types (SessionOutcomeRecord) that map 1:1 to database columns for better maintainability. _(spec: 105_db_plan_state, confidence: 0.60)_
  - cause: Update both base schema DDL and migration functions when int…
  - effect: Allow nullable/optional fields in dataclass records (using |…
- Implement lazy file opening (on first write) to handle directory creation gracefully and support fallback logic when method-level identifiers are empty. _(spec: 103_agent_conversation_trace, confidence: 0.60)_
  - cause: For JSONL event logging, include a timestamp field as ISO fo…
  - effect: Use TYPE_CHECKING guards with if TYPE_CHECKING blocks to avo…
- Property tests should use Hypothesis @st.composite strategies for complex objects like signal lists and profiles; assume() should filter invalid cases (e.g., unknown archetypes). _(spec: 104_adaptive_retrieval, confidence: 0.60)_
  - effect: Token budget compliance in context assembly should be enforc…
- Property tests for signal degradation should verify that retrieval pipelines handle any combination of empty/non-empty signals without raising exceptions. _(spec: 104_adaptive_retrieval, confidence: 0.60)_
  - cause: RRF (Reciprocal Rank Fusion) score formula is weight / (k + …
  - effect: RetrievalResult must include signal_counts (dict of signal_n…
- Property-based tests using Hypothesis generate run_id values matching the format YYYYMMDD_HHMMSS_hhhhhhh (8 digits, underscore, 6 digits, underscore, 6 hex chars) to ensure test coverage of realistic id formats. _(spec: 103_agent_conversation_trace, confidence: 0.60)_
  - cause: Integration smoke tests use a MockBackend class that yields …
  - effect: Test specs include placeholder assertions (e.g., in test_age…
- Property-based testing with Hypothesis should be used to verify invariants like event completeness, truncation behavior, and file location correctness across variable inputs. _(spec: 103_agent_conversation_trace, confidence: 0.60)_
  - cause: Test files should be organized by type: unit tests in tests/…
- Missing DB paths in status commands should return None or an empty dict rather than raising exceptions, allowing graceful fallback to 'No plan found' messages. _(spec: 105_db_plan_state, confidence: 0.60)_
- Error classes that are part of the public API should be tracked in property-based tests to ensure consistency across error class hierarchies. _(spec: 103_remove_hook_runner, confidence: 0.60)_
- When removing hook execution from async functions, update docstrings and step numbering to reflect the new process flow and remove references to deleted requirements. _(spec: 103_remove_hook_runner, confidence: 0.60)_
  - cause: ---
Title: 03. Adopt iterative two-phase fix architecture
Da…
- When testing backward compatibility with deprecated config sections (e.g., `[hooks]` TOML), parametrize multiple variants including empty sections, partial fields, and unknown keys. _(spec: 103_remove_hook_runner, confidence: 0.60)_
  - cause: Define banned symbols or patterns as module-level constants …
- Define banned symbols or patterns as module-level constants (e.g., `_BANNED_SYMBOLS`) to centralize the list and make it reusable across multiple test cases. _(spec: 103_remove_hook_runner, confidence: 0.60)_
  - cause: Use `Path.rglob('*.py')` to recursively find all Python file…
  - effect: When testing backward compatibility with deprecated config s…
- When verifying a config reload operation, test both the success case (returns ReloadResult) and the skip case (returns None when config_path is None). _(spec: 103_remove_hook_runner, confidence: 0.60)_
- Write smoke tests as integration tests that exercise end-to-end behavior (e.g., security hook blocking/allowing decisions) without mocking internal dependencies. _(spec: 103_remove_hook_runner, confidence: 0.60)_
  - cause: ---
Title: 03. Adopt iterative two-phase fix architecture
Da…

## Decisions

- Task group completion state should be tracked in a dedicated state file (plan.json) rather than in markdown checkboxes, as checkbox state in markdown is a presentation concern and fragile for reliable state tracking. _(spec: 110_hunt_dedup_and_ignore, confidence: 0.90)_
  - effect: Specs must remain immutable during execution to preserve the…
- The filter_ignored function uses strict greater-than (>) comparison for similarity thresholds, meaning a threshold of 0.0 matches any positive similarity, and at-threshold similarities do not suppress groups. _(spec: 111_rich_memory_rendering, confidence: 0.90)_
  - cause: A constant-vector embedder (returning the same embedding for…
  - cause: Batch embedding calls can be optimized by concatenating mult…
  - effect: Fail-open behavior (returning all unfiltered groups on error…
- For None/empty handling in vector operations, define behavior as returning 0.0 for cosine similarity when either input is None or empty, and test both argument positions. _(spec: 111_rich_memory_rendering, confidence: 0.90)_
  - cause: Use pytest.approx() for floating-point comparisons and alway…
- Invoke the AI fix pipeline at most once per run, even if re-validation produces new findings, to prevent infinite loops and ensure single-pass processing guarantees. _(spec: 109_ai_fix_pipeline_wiring, confidence: 0.90)_
  - effect: Isolate errors in multi-spec pipelines by catching exception…
- Dependency validators must include archived specs in their resolution scope to correctly handle transitions where specs move from active to archived status after full implementation. _(spec: 111_rich_memory_rendering, confidence: 0.90)_
  - cause: When building a specs dependency map, scan both active and a…
- PASS verdicts delete existing audit reports rather than overwriting them. _(spec: 92_transient_audit_reports, confidence: 0.90)_
  - effect: When audit report paths change, existing tests must be updat…
- Pre-review mode focuses on spec quality before coding and prohibits code inspection; drift-review and audit-review modes are read-only code inspection; fix-review mode is the only mode that runs tests and validates implementation. _(spec: 97_archetype_modes, confidence: 0.90)_
- Each reviewer mode has specific constraints: pre-review and drift-review are read-only (ls, cat, git only), fix-review can run tests and linters, audit-review can run pytest --collect-only and limited test execution for a specific task group. _(spec: 97_archetype_modes, confidence: 0.90)_
- Apply both .gitignore and .night-shift patterns additively in a single combined spec, rather than checking them separately, for consistent and predictable exclusion behavior. _(spec: 108_issue_session_summary, confidence: 0.90)_
  - cause: When using pathspec for gitignore-style matching, maintain t…
- When filtering collections with a removal predicate, preserve items with empty collections (e.g., findings with no affected_files) as-is, but drop items whose collections become empty after filtering. _(spec: 108_issue_session_summary, confidence: 0.90)_
  - effect: Use frozen dataclasses with the `replace()` function from da…
- The pathspec library (version >=0.12) is required for implementing ignore specifications in the nightshift module. _(spec: 106_nightshift_ignore, confidence: 0.90)_
  - effect: NightShiftIgnoreSpec dataclass should be paired with load_ig…
- When a finding's affected_files list becomes empty after filtering, drop the entire finding from results (do not preserve it). _(spec: 106_nightshift_ignore, confidence: 0.90)_
  - effect: For filtering operations that remove items, verify the monot…
- Default exclusions (hardcoded patterns) should not be overrideable by user configuration, even via negation patterns in gitignore-style formats. _(spec: 106_nightshift_ignore, confidence: 0.90)_
  - cause: Pathspec library requires patterns to use forward slashes (P…
- The Elixir analyzer is a special case that must never produce CLASS entities or EXTENDS edges, reflecting the language's functional paradigm without class inheritance. _(spec: 107_additional_language_analyzers, confidence: 0.90)_
  - effect: Entity extraction must produce FILE entities with the filena…
- When removing JSONL infrastructure like memory.jsonl, remove the file reference from .gitignore and delete all related export/import functions to complete the migration to database-first architecture. _(spec: 104_adaptive_retrieval, confidence: 0.90)_
- JSONL export functionality should be removed when moving to database-first architecture, since DB provides the canonical fact storage and export adds synchronization complexity. _(spec: 104_adaptive_retrieval, confidence: 0.90)_
  - effect: Document breaking changes and backward-compatibility decisio…
- Per-node status should be persisted to the database on each state transition rather than batched at end-of-run to maintain consistency and enable recovery from intermediate failures. _(spec: 105_db_plan_state, confidence: 0.90)_
  - effect: Run-level aggregations like total tokens and cost should be …
- All cross-spec entry points must be verified to receive DuckDB connections rather than file paths to ensure a single source of truth and prevent file I/O conflicts. _(spec: 105_db_plan_state, confidence: 0.90)_
  - effect: Database operations in result handlers should be wrapped in …
- When persisting node status per-transition to a database, retrieve the current status from the graph state manager rather than using stale values from incoming records. _(spec: 105_db_plan_state, confidence: 0.90)_
  - cause: Database operations in critical paths should be wrapped in t…
- When removing widely-imported constants/classes from production code, retain them as backward-compatible shims if 25+ test files depend on them directly, rather than rewriting all tests simultaneously to avoid large-scale regressions. _(spec: 105_db_plan_state, confidence: 0.90)_
  - cause: ---
Title: 05. Adopt MAGMA multi-graph memory architecture
D…
- RRF (Reciprocal Rank Fusion) score formula is weight / (k + rank), where k is a configurable parameter (default 60); facts appearing in multiple signals accumulate scores from each. _(spec: 104_adaptive_retrieval, confidence: 0.90)_
  - cause: Causal ordering must be enforced in context assembly: predec…
  - cause: Intent profiles (IntentProfile class) map archetype+node_sta…
  - effect: Property tests for signal degradation should verify that ret…
- Large prompt fields (system_prompt, task_prompt) should be captured verbatim without truncation to preserve full context for auditing. _(spec: 105_db_plan_state, confidence: 0.90)_
  - effect: String truncation in tool_input should preserve the original…
- JsonlSink is being removed and replaced with AgentTraceSink; tests should be written to verify the removal in group 3 by asserting the module no longer exists and no imports remain. _(spec: 103_agent_conversation_trace, confidence: 0.90)_
  - cause: Audit retention cleanup (enforce_audit_retention) should del…
  - effect: Non-string values in tool_input dictionaries (integers, bool…
- Audit retention cleanup (enforce_audit_retention) should delete audit_*.jsonl files when pruning runs, but preserve agent_*.jsonl files even for pruned runs. _(spec: 103_agent_conversation_trace, confidence: 0.90)_
  - effect: JsonlSink is being removed and replaced with AgentTraceSink;…
- Long prompts (system_prompt and task_prompt) in session.init events should be captured verbatim without any truncation, unlike tool inputs. _(spec: 103_agent_conversation_trace, confidence: 0.90)_
  - cause: The truncate_tool_input function truncates string values exc…
- NodeStatus enum must include v3-specific values like 'cost_blocked' and 'merge_blocked' in addition to the standard status values. _(spec: 105_db_plan_state, confidence: 0.90)_
  - cause: Crash recovery requires a `reset_in_progress_nodes()` functi…
- Dead code with explicit return-None statements may be justified in specific contexts (e.g., _format_relative_age); document the rationale rather than removing without understanding intent. _(spec: 111_rich_memory_rendering, confidence: 0.60)_
  - effect: Comprehensive verification should include design specificati…
- When removing path constants from a centralized module, inline local path definitions in calling code rather than creating a new centralized import, to avoid cascading dependency issues. _(spec: 104_adaptive_retrieval, confidence: 0.60)_
  - effect: .gitignore rules that exclude certain files from general ign…

## Conventions

- All intentional stubs and early-exit returns should be explicitly documented during code audits with rationale to distinguish them from unimplemented dead code. _(spec: 110_hunt_dedup_and_ignore, confidence: 0.90)_
  - cause: Optional return types (like `str | None`) should be document…
  - cause: Early-exit guard clauses returning empty lists (e.g., `retur…
  - effect: Wiring verification requires tracing data flow through the c…
- Optional return types (like `str | None`) should be documented with comments explaining when None is returned; `extract_fingerprint()` returns None when no marker is found, which is a valid Optional return and not a stub. _(spec: 110_hunt_dedup_and_ignore, confidence: 0.90)_
  - effect: All intentional stubs and early-exit returns should be expli…
- Inline tags on task groups use [key: value] format with colon-space separator to distinguish from markdown link syntax, and duplicate tag keys on a single group are a validation error. _(spec: 110_hunt_dedup_and_ignore, confidence: 0.90)_
  - effect: Task group numbering must be sequential with no gaps (1, 2, …
- Task group numbering must be sequential with no gaps (1, 2, 3) and the parser should auto-fix renumbered groups including subtask prefixes and dependency table references. _(spec: 110_hunt_dedup_and_ignore, confidence: 0.90)_
  - cause: Requirement IDs should follow the pattern [NN-REQ-N.M] where…
  - cause: Inline tags on task groups use [key: value] format with colo…
  - effect: File layout annotations in design.md should include [group N…
- Requirement IDs should follow the pattern [NN-REQ-N.M] where NN is the spec's two-digit numeric prefix, N is the section number, and M is the requirement within that section to ensure unique, traceable identifiers across specs. _(spec: 110_hunt_dedup_and_ignore, confidence: 0.90)_
  - effect: Task group numbering must be sequential with no gaps (1, 2, …
- Design documents should explicitly name concrete file paths (e.g., 'src/auth/middleware.py') rather than abstract references to enable file impact prediction and knowledge fact association with source files. _(spec: 110_hunt_dedup_and_ignore, confidence: 0.90)_
  - effect: File layout annotations in design.md should include [group N…
- Design documents should use explicit file paths rather than abstract references, as this feeds file impact prediction, merge conflict prevention, and knowledge staleness tracking systems. _(spec: 110_hunt_dedup_and_ignore, confidence: 0.90)_
- The CLI entry point (cli/nightshift.py) is responsible for creating the EmbeddingGenerator instance and passing it to the engine, maintaining separation of concerns between CLI initialization and engine logic. _(spec: 110_hunt_dedup_and_ignore, confidence: 0.90)_
  - cause: Anti_pattern facts stored in DuckDB should be passed through…
- Comprehensive verification should include design specification alignment (design.md), backward compatibility checks with all caller modules, test suite execution, and code quality audits before marking a task complete. _(spec: 111_rich_memory_rendering, confidence: 0.90)_
  - cause: When modifying function signatures for backward compatibilit…
  - cause: Property-based tests can reveal duplicate-content bugs that …
- Integration smoke tests should use real components without stub bypasses and must pass alongside unit and property tests to confirm wiring correctness. _(spec: 109_ai_fix_pipeline_wiring, confidence: 0.90)_
- Early returns of empty lists or None on non-Optional types are acceptable if they represent intentional guard conditions or early exits documented in design specs, but must include rationale comments. _(spec: 109_ai_fix_pipeline_wiring, confidence: 0.90)_
- Accept similarity_threshold and embedder as configurable parameters in deduplication functions to enable flexible tuning for different use cases. _(spec: 110_hunt_dedup_and_ignore, confidence: 0.90)_
  - cause: Use batch embedding with cosine similarity comparison for ne…
- Define label constants (name and color hex) alongside their LabelSpec entries in a centralized labels module to maintain consistency across the platform. _(spec: 110_hunt_dedup_and_ignore, confidence: 0.90)_
- Boundary threshold tests (0.0, 1.0) should have dedicated test classes that verify suppression behavior at the extremes (TS-110-E8, TS-110-E9). _(spec: 110_hunt_dedup_and_ignore, confidence: 0.90)_
  - cause: Test specifications should include test case IDs (e.g., TS-1…
- Edge case tests should have dedicated test classes with clear naming (e.g., `TestNoEmbedderFallback` for TS-110-E2) and document the specific failure mode being tested. _(spec: 110_hunt_dedup_and_ignore, confidence: 0.90)_
  - cause: Mock embedders should implement an `embed_batch_call_count` …
  - cause: Platform mock classes should support both successful and fai…
- Hypothesis property tests should be marked with `@pytest.mark.property` and use `@settings(max_examples=N)` to control test iteration count for reproducibility. _(spec: 110_hunt_dedup_and_ignore, confidence: 0.90)_
  - cause: Helper functions like `_make_finding()` and `_make_group()` …
  - effect: When testing async functions with property-based tests, wrap…
- Test specifications should include test case IDs (e.g., TS-110-4) in docstrings and be organized by requirement number and category (unit, integration, edge case, property, smoke). _(spec: 110_hunt_dedup_and_ignore, confidence: 0.90)_
  - effect: Boundary threshold tests (0.0, 1.0) should have dedicated te…
  - effect: Edge case tests should have dedicated test classes with clea…
- Test docstrings should include the test case identifier (e.g., TS-110-4) at the start of the class docstring, with individual test methods describing the specific assertion. _(spec: 111_rich_memory_rendering, confidence: 0.90)_
  - cause: Test files should use helper functions like _make_finding() …
  - cause: When testing async functions with asyncio, create a new even…
- Use pytest.approx() for floating-point comparisons and always specify abs= or rel= tolerance to handle numerical precision issues. _(spec: 111_rich_memory_rendering, confidence: 0.90)_
  - effect: For None/empty handling in vector operations, define behavio…
- Edge case tests should be organized in separate test classes (e.g., TestNoEmbedderFallback, TestPlatformFailureFallsOpen) and clearly labeled with their test case identifiers (e.g., TS-110-E2). _(spec: 111_rich_memory_rendering, confidence: 0.90)_
  - cause: Mock embedders for testing should implement an embed_batch()…
- Integration tests for spec linting should verify that dependencies on archived specs do not produce false-positive broken-dependency violations. _(spec: 110_hunt_dedup_and_ignore, confidence: 0.90)_
  - cause: When merging specs from multiple directories into a lookup m…
- When restoring or deferring a field implementation, document the decision and current usage status in an errata file to clarify why the field exists but may not be actively used in production code. _(spec: 99_archetype_profiles, confidence: 0.90)_
  - effect: Unit tests for partial overrides should explicitly verify bo…
- When verifying implementation completeness, ensure no unjustified stubs remain in the codebase as they indicate incomplete implementation. _(spec: 95_entity_graph, confidence: 0.90)_
- When a spec requirement is temporarily deferred, document the deviation explicitly with an errata file explaining what was missing, why, and how it was resolved. _(spec: 98_reviewer_consolidation, confidence: 0.90)_
- Use `field(default_factory=list)` for mutable default values in dataclasses instead of `None`, and reserve `None` for optional fields that allow inheritance/override behavior. _(spec: 98_reviewer_consolidation, confidence: 0.90)_
  - effect: When adding a field to a dataclass used in `dataclasses.repl…
- Reserved fields that are not yet consumed by production code should be documented as such, with guidance on how future specs should use them rather than introducing new fields. _(spec: 93_fix_branch_push, confidence: 0.90)_
  - effect: Use field-level mapping documentation (like _MODE_TO_ENTRY_F…
- Task group 4 (fact-entity linking and graph queries) for spec 95 (entity_graph) was already fully implemented in a prior session with all 27 tests passing and clean linter output, requiring no additional work. _(spec: 95_entity_graph, confidence: 0.90)_
- Reserved/future fields in dataclasses should be explicitly documented in code comments and errata files to prevent them from being accidentally removed or ignored in future implementation passes. _(spec: 97_archetype_modes, confidence: 0.90)_
  - effect: When adding fields to dataclasses used in property-based tes…
- When restoring removed fields due to spec violations, create an errata document explaining the deviation, resolution, design rationale, and impact to help future maintainers understand the change. _(spec: 92_transient_audit_reports, confidence: 0.90)_
  - cause: When adding a new field to a dataclass that participates in …
  - cause: Fields reserved for future use should still be added with ap…
- Fields reserved for future use should still be added with appropriate defaults and include a design note explaining why they exist but are unused by current production code. _(spec: 92_transient_audit_reports, confidence: 0.90)_
  - effect: When restoring removed fields due to spec violations, create…
- Use commit hashes and test suite status (unit tests, smoke tests, linter) to verify that a task group is fully complete before starting implementation work. _(spec: 95_entity_graph, confidence: 0.90)_
  - cause: When a feature branch is at the same commit as develop and a…
- Use `field(default_factory=list)` for list fields in dataclasses to avoid mutable default arguments, while `None` is appropriate for optional override fields that represent inheritance. _(spec: 96_knowledge_consolidation, confidence: 0.90)_
  - cause: When restoring a previously removed field to satisfy spec re…
  - effect: When a field can be overridden at multiple levels (base entr…
- ArchetypeEntry.templates is reserved for future use; current template specification mechanism uses profile files from spec 99. _(spec: 97_archetype_modes, confidence: 0.90)_
- Audit reports for transient_audit_reports are written to `.agent-fox/audit/audit_{spec_name}.md` with auto-creation of the audit directory. _(spec: 92_transient_audit_reports, confidence: 0.90)_
  - effect: When audit report paths change, existing tests must be updat…
- Audit-review mode verdicts use a four-value system (PASS, WEAK, MISSING, MISALIGNED) per test spec entry; overall verdict is FAIL if any MISSING or MISALIGNED entry exists, or if 2+ WEAK entries are found. _(spec: 97_archetype_modes, confidence: 0.90)_
- Severity levels for findings use a four-tier system (critical, major, minor, observation); critical severity typically indicates blocking issues that must be resolved before proceeding. _(spec: 97_archetype_modes, confidence: 0.90)_
- Reviewer modes are constrained to specific tool usage: pre-review and drift-review use only file utilities (ls, cat, git, head, tail, wc); fix-review and audit-review may run pytest; none may modify code. _(spec: 97_archetype_modes, confidence: 0.90)_
- Reviewer modes use read-only file operations (ls, cat, git, grep, find) and are forbidden from creating, modifying, or deleting source files, with different modes having specific command restrictions (drift-review forbids grep/find). _(spec: 95_entity_graph, confidence: 0.90)_
- Intermediate analysis messages between tool calls may contain prose, but only the final message is parsed; everything before the final JSON object is discarded by the system. _(spec: 92_transient_audit_reports, confidence: 0.90)_
- Reviewer findings must be structured and evidence-based with references to specific artifacts (requirement IDs, file paths, test functions); vague observations like 'consider adding more tests' are not valid findings and should be omitted. _(spec: 92_transient_audit_reports, confidence: 0.90)_
- Drift-review findings use severity levels (critical, major, minor, observation) and must reference specific requirements, design decisions, or code artifacts; vague observations are not findings and should be omitted. _(spec: 97_archetype_modes, confidence: 0.90)_
  - cause: Reviewer roles operate in distinct modes (pre-review, fix-re…
  - effect: All reviewer output JSON fields must use exact names as spec…
- Audit-review verdicts per test spec entry are PASS (adequate coverage), WEAK (exists but insufficient), MISSING (no test), or MISALIGNED (wrong scenario); overall verdict is FAIL if any MISSING, any MISALIGNED, or 2+ WEAK entries. _(spec: 97_archetype_modes, confidence: 0.90)_
  - cause: Reviewer constraints vary by mode: pre-review and drift-revi…
  - cause: Reviewer roles operate in distinct modes (pre-review, fix-re…
- Reviewer operations use read-only access patterns: drift-review uses only ls/cat/git/grep/find/head/tail; audit-review uses pytest collection and execution; pre-review excludes grep and find; fix-review may run make/pytest/ruff but not modify source. _(spec: 92_transient_audit_reports, confidence: 0.90)_
- All reviewer profiles are read-only and prohibit implementation or modification; they may only review and report findings using constrained tool sets (ls, cat, grep, find) specific to each review mode. _(spec: 95_entity_graph, confidence: 0.90)_
- Reviewer output JSON must use exact field names per mode (findings, drift_findings, verdicts, audit) and intermediate analysis text between tool calls is discarded; only the final message is parsed. _(spec: 97_archetype_modes, confidence: 0.90)_
- Private helper functions with Optional return types can legitimately return None, but this must be explicit in the type signature; this pattern is acceptable and does not require additional justification. _(spec: 107_additional_language_analyzers, confidence: 0.90)_
- Adding a new language analyzer requires declaring its tree-sitter dependency (e.g., tree-sitter-kotlin) in pyproject.toml. _(spec: 107_additional_language_analyzers, confidence: 0.90)_
  - effect: Language analyzers should be registered in a central registr…
- Language analyzers should be registered in a central registry after implementation to make them discoverable by the agent system. _(spec: 107_additional_language_analyzers, confidence: 0.90)_
  - cause: Language analyzers need to extract both entities (FILE, MODU…
  - cause: Adding a new language analyzer requires declaring its tree-s…
- Tree-sitter language parsers should be specified with version constraints (e.g., tree-sitter-elixir>=0.3.4) in pyproject.toml dependencies. _(spec: 107_additional_language_analyzers, confidence: 0.90)_
  - cause: Language analyzers should implement the LanguageAnalyzer pro…
- The nightshift.ignore module provides load_ignore_spec and filter_findings functions that should be called after gathering all findings to filter results. _(spec: 106_nightshift_ignore, confidence: 0.90)_
  - cause: When integrating new modules with existing scanners, wrap th…
- Initialize new optional fields in dataclasses with sensible defaults (e.g., `= "skipped"`) to maintain backward compatibility when extending existing result objects. _(spec: 108_issue_session_summary, confidence: 0.90)_
  - cause: Use frozen dataclasses with the `replace()` function from da…
- External language parsing dependencies (like tree-sitter-c-sharp) should be added to pyproject.toml rather than handled separately. _(spec: 107_additional_language_analyzers, confidence: 0.90)_
  - effect: When adding a new language analyzer, register it in the defa…
- File extensions across all registered analyzers must be pairwise disjoint with no overlap, enforced by checking during registry initialization. _(spec: 107_additional_language_analyzers, confidence: 0.90)_
  - effect: Integration smoke tests should verify that analyze_codebase …
- Entity extraction must produce FILE entities with the filename as entity_name for all language analyzers, and all non-MODULE entities should have entity_path equal to the relative file path. _(spec: 107_additional_language_analyzers, confidence: 0.90)_
  - cause: All LanguageAnalyzer implementations must have non-empty lan…
  - cause: The Elixir analyzer is a special case that must never produc…
- Module map values must always be POSIX-style relative paths (forward slashes, no leading slash, no backslashes) regardless of the platform, even on Windows. _(spec: 107_additional_language_analyzers, confidence: 0.90)_
  - effect: Integration smoke tests should verify that analyze_codebase …
- Organize failing tests across multiple test file types (unit, property, integration) and track completion with task checkboxes to manage spec implementation work systematically. _(spec: 106_nightshift_ignore, confidence: 0.90)_
  - cause: When implementing spec tests before implementation, verify t…
  - effect: When multiple specs have pre-existing test failures, clearly…
- Use dataclasses for structured data containers like SourceIssue to provide clear type definitions and automatic __init__ generation. _(spec: 108_issue_session_summary, confidence: 0.90)_
  - cause: Async functions that iterate over collections and perform I/…
- Use a naming convention like TS-107-1, TS-107-E9 for unit tests, TS-107-P1 through TS-107-P7 for property tests, and TS-107-SMOKE-1 for integration tests to maintain traceability between spec requirements and test cases. _(spec: 107_additional_language_analyzers, confidence: 0.90)_
  - cause: When implementing support for multiple programming languages…
  - effect: When writing tests before implementation (TDD), it is expect…
- Pre-release versions use the semantic versioning format 'X.Y.ZrcN' (e.g., '3.0.0rc6') to denote release candidates before final release. _(spec: 107_additional_language_analyzers, confidence: 0.90)_
  - effect: Documentation files (like memory.md) that reference version …
- Version numbers must be synchronized across multiple files: __init__.py, pyproject.toml, and docs/memory.md to maintain consistency across the codebase. _(spec: 107_additional_language_analyzers, confidence: 0.90)_
  - effect: Documentation files (like memory.md) that reference version …
- Pre-release versions use the semantic versioning format 'X.Y.ZrcN' (e.g., '3.0.0rc6') to denote release candidates. _(spec: 108_issue_session_summary, confidence: 0.90)_
  - effect: When updating version numbers, ensure all references in docu…
- Document breaking changes and backward-compatibility decisions in errata files (e.g., docs/errata/105_db_plan_state.md) to explain why legacy code wasn't fully removed. _(spec: 104_adaptive_retrieval, confidence: 0.90)_
  - cause: JSONL export functionality should be removed when moving to …
  - cause: Use try-except with graceful fallback (DB first, file second…
- Justified stubs (like returning empty lists for valid absence cases or returning None per type annotation) should be explicitly documented with their justification to distinguish them from incomplete implementations. _(spec: 105_db_plan_state, confidence: 0.90)_
- Update database schema version assertions in property tests when adding new migration versions; increment the expected version count and document which migrations added which tables. _(spec: 105_db_plan_state, confidence: 0.90)_
  - cause: ---
Title: 04. Block coding sessions on critical review find…
- Document spec divergences (like retaining `StateManager` despite removal requirement) in a dedicated errata file with full rationale, listing affected test files and the decision to preserve backward compatibility. _(spec: 105_db_plan_state, confidence: 0.90)_
  - cause: ---
Title: 03. Adopt iterative two-phase fix architecture
Da…
- Reorganize imports to follow a consistent pattern: standard library, third-party packages, then local imports, separated by blank lines. _(spec: 104_adaptive_retrieval, confidence: 0.90)_
- Format collection literals (sets, dicts) with one item per line for better readability and diff clarity, especially for lists of file extensions or configuration values. _(spec: 104_adaptive_retrieval, confidence: 0.90)_
- Consistently format long f-strings and multi-line string concatenations onto a single line when they exceed typical line length but remain readable, rather than splitting across multiple lines. _(spec: 104_adaptive_retrieval, confidence: 0.90)_
- Tests that depend on incomplete implementation should use `@pytest.mark.xfail(strict=True)` with a clear reason and reference to the task that will complete the work. _(spec: 105_db_plan_state, confidence: 0.90)_
- Store NULL in the database (not empty string) for error_message when a session succeeds, allowing proper SQL queries with `IS NULL` checks. _(spec: 105_db_plan_state, confidence: 0.90)_
  - cause: Use `hasattr(value, 'isoformat')` to safely convert datetime…
- Invert assertion logic in verification tests when moving from 'current state' tests to 'desired end state' tests; document the transition clearly to avoid confusion about test intent. _(spec: 103_agent_conversation_trace, confidence: 0.90)_
- Dataclass fields representing database NULL values should use Optional types (e.g., `str | None`) to distinguish between missing and present data, with comments documenting SQL NULL behavior. _(spec: 104_adaptive_retrieval, confidence: 0.90)_
- Use TYPE_CHECKING guards with if TYPE_CHECKING blocks to avoid circular imports while maintaining proper type hints for protocol-based duck typing. _(spec: 103_agent_conversation_trace, confidence: 0.90)_
  - cause: Implement lazy file opening (on first write) to handle direc…
- RetrievalResult must include signal_counts (dict of signal_name → count), anchor_count (number of facts selected), intent_profile, and context (string with provenance metadata like 'spec:'). _(spec: 104_adaptive_retrieval, confidence: 0.90)_
  - cause: Property tests for signal degradation should verify that ret…
  - cause: Token budget compliance in context assembly should be enforc…
- Keyword signal scores facts matching the spec_name exactly, then adds bonus scoring for keyword overlap; confidence filtering is applied before ranking. _(spec: 104_adaptive_retrieval, confidence: 0.90)_
  - cause: Entity signal performs BFS traversal of entity_graph to find…
  - effect: Failing tests should be created first (red phase) before imp…
- Vector signal requires an embedder with embedding_dimensions property and embed_text() method; results should be ordered by cosine similarity in descending order. _(spec: 104_adaptive_retrieval, confidence: 0.90)_
  - cause: Unit test fixtures should include UUIDs for facts (FACT_AAA,…
  - effect: Entity signal performs BFS traversal of entity_graph to find…
- Unit test fixtures should include UUIDs for facts (FACT_AAA, FACT_BBB, etc.) and mock embeddings (MOCK_EMBEDDING_1, MOCK_EMBEDDING_2, etc.) shared across test files via conftest. _(spec: 104_adaptive_retrieval, confidence: 0.90)_
  - effect: Vector signal requires an embedder with embedding_dimensions…
- Intent profiles (IntentProfile class) map archetype+node_status pairs to weighted signal preferences; unknown archetypes should fall back to balanced default (all weights 1.0). _(spec: 104_adaptive_retrieval, confidence: 0.90)_
  - effect: RRF (Reciprocal Rank Fusion) score formula is weight / (k + …
- Confidence thresholds in knowledge retrieval are used to filter facts; only facts meeting the confidence threshold should be returned by signal functions. _(spec: 104_adaptive_retrieval, confidence: 0.90)_
  - cause: Integration tests for retrieval pipelines should mock the em…
  - effect: Causal ordering must be enforced in context assembly: predec…
- Trace file paths are constructed as audit_dir / f"agent_{run_id}.jsonl" and this canonical path must be checked after every write to verify correct file location. _(spec: 103_agent_conversation_trace, confidence: 0.90)_
  - cause: AgentTraceSink writes trace events to JSONL files at audit_d…
- Test specs include placeholder assertions (e.g., in test_agent_trace_removal.py) that will be inverted in task group 3 to verify JsonlSink module removal and import cleanup. _(spec: 103_agent_conversation_trace, confidence: 0.90)_
  - cause: Property-based tests using Hypothesis generate run_id values…
- Test files should be organized by type: unit tests in tests/unit/, property tests in tests/property/, and integration smoke tests in tests/integration/. _(spec: 103_agent_conversation_trace, confidence: 0.90)_
  - effect: Property-based testing with Hypothesis should be used to ver…
- Import statements for not-yet-implemented modules should use `# noqa: F401` comments to suppress linter warnings in spec test files that serve as forward contracts. _(spec: 105_db_plan_state, confidence: 0.90)_
- Schema DDL should be defined as a module-level constant string and reused across multiple test files to ensure consistency and reduce duplication. _(spec: 105_db_plan_state, confidence: 0.90)_
  - effect: Test fixtures using in-memory DuckDB (`:memory:`) are suitab…
- Pre-release versions follow the semantic versioning format with rc suffix (e.g., 3.0.0rc5) to indicate release candidate status. _(spec: 104_adaptive_retrieval, confidence: 0.90)_
  - cause: Version numbers must be synchronized across multiple files: …
- Release candidates should use semantic versioning format with 'rc' suffix (e.g., 3.0.0rc5) to indicate pre-release versions. _(spec: 103_agent_conversation_trace, confidence: 0.90)_
  - cause: Version numbers must be kept in sync across multiple files: …
- Release candidate versions use the format 'X.Y.ZrcN' (e.g., '3.0.0rc5') to indicate pre-release status before final release. _(spec: 104_adaptive_retrieval, confidence: 0.90)_
  - cause: Version numbers must be synchronized across multiple files: …
- Pre-release versions use the format 'X.Y.ZrcN' (e.g., '3.0.0rc5') to denote release candidates. _(spec: 103_agent_conversation_trace, confidence: 0.90)_
  - cause: Version strings must be kept synchronized across multiple fi…
- Version numbers must be synchronized across multiple files: __init__.py, pyproject.toml, and uv.lock to maintain consistency. _(spec: 105_db_plan_state, confidence: 0.90)_
  - effect: When updating package versions, ensure all three locations (…
- When relocating a module, update the logger name to match the new package path (e.g., 'agent_fox.hooks.security' → 'agent_fox.security.security') to ensure proper log filtering and tracing. _(spec: 103_remove_hook_runner, confidence: 0.90)_
  - effect: When moving test files during a module relocation, ensure al…
- Use `# noqa: PLC0415` comment on local imports inside test functions to suppress linter warnings about imports not at module level, which is common practice in test fixtures. _(spec: 103_remove_hook_runner, confidence: 0.90)_
  - cause: Use `@pytest.mark.parametrize` with a list of test variants …
- Use `noqa: PLC0415` comments on late imports inside test methods to suppress linter warnings when imports are intentionally deferred for test isolation. _(spec: 103_remove_hook_runner, confidence: 0.90)_
- ADR consequences should explicitly include negative trade-offs and accepted risks; omitting downsides signals incomplete analysis or overconfidence. _(spec: 103_remove_hook_runner, confidence: 0.90)_
  - cause: ADRs should document at least one real alternative with hone…
- ADRs should document at least one real alternative with honest trade-offs; a one-sided ADR that only praises the chosen option is not trustworthy and should be rejected during quality review. _(spec: 103_remove_hook_runner, confidence: 0.90)_
  - cause: When creating ADRs, the code is the single source of truth a…
  - effect: ADR consequences should explicitly include negative trade-of…
- Architecture Decision Records (ADRs) should be stored in a conventional directory structure (e.g., docs/adr/, doc/adr/, adr/) with numerically-prefixed filenames (NN-imperative-verb-phrase.md) to maintain ordering and discoverability. _(spec: 103_remove_hook_runner, confidence: 0.90)_
  - effect: When creating ADRs, the code is the single source of truth a…
- When implementing a feature with multiple test categories (unit, property, integration), organize tests by spec entry identifiers (e.g., TS-111-1) to maintain traceability between requirements and test coverage. _(spec: 111_rich_memory_rendering, confidence: 0.60)_
  - cause: Critical domain findings (like case-sensitive entity type va…
- Spec dependency documentation should reference the group number where a dependency was actually implemented, not where verification occurred, to maintain accurate traceability. _(spec: 109_ai_fix_pipeline_wiring, confidence: 0.60)_
  - cause: When iterating through multiple directories with overlapping…
  - effect: Integration tests for spec linting should include cases for …
- Fields that map 1:1 between dataclasses (e.g., ModeConfig.templates → ArchetypeEntry.templates) should be documented in comments alongside transformation mappings for clarity. _(spec: 98_reviewer_consolidation, confidence: 0.60)_
  - cause: When updating property-based tests for dataclass field chang…
- Mark fields that are reserved for future use with explicit comments referencing the spec requirement number and note when production code paths do not currently consume them. _(spec: 96_knowledge_consolidation, confidence: 0.60)_
- Test directory locations are language-dependent and should not be assumed to follow a single convention across projects. _(spec: 110_hunt_dedup_and_ignore, confidence: 0.60)_
- Test directory locations are language-dependent and should not be assumed to follow a single convention across the codebase. _(spec: 109_ai_fix_pipeline_wiring, confidence: 0.60)_
- Test directory locations are language-dependent and should be verified for each project rather than assumed to follow a standard pattern. _(spec: 111_rich_memory_rendering, confidence: 0.60)_
- Idempotent operations like pre-existing no-op branches should be preserved even when unrelated to the current spec; mark them as intentional to avoid future confusion. _(spec: 106_nightshift_ignore, confidence: 0.60)_
  - cause: ---
Title: 03. Adopt iterative two-phase fix architecture
Da…
- When implementing features that modify project structure (like creating configuration files), provide output in both human-readable text format and machine-readable JSON format in the CLI layer. _(spec: 106_nightshift_ignore, confidence: 0.60)_
- Task completion tracking in spec documents should mark items as complete (checked) only after all test assertions exist and tests run to completion, even if implementation is incomplete. _(spec: 108_issue_session_summary, confidence: 0.60)_
  - effect: The memory.md file serves as a knowledge base that accumulat…
- Remove xfail markers from tests once their implementation is complete, even if the feature involved incomplete work elsewhere; this prevents test suite confusion and keeps test status accurate. _(spec: 105_db_plan_state, confidence: 0.60)_
  - cause: ---
Title: 04. Block coding sessions on critical review find…
- Break long assertion messages and f-string formatting across lines only when necessary; prefer single-line format when the entire expression fits reasonably within the character limit. _(spec: 104_adaptive_retrieval, confidence: 0.60)_
- Remove blank lines between adjacent import statements in the same group and between docstring and code in test files for consistency. _(spec: 104_adaptive_retrieval, confidence: 0.60)_
- Allow nullable/optional fields in dataclass records (using | None) only when the database permits NULL values for that column, and document the reason (e.g., error_message is NULL for successful sessions). _(spec: 105_db_plan_state, confidence: 0.60)_
  - cause: Consolidate scattered dataclass fields (like SessionRecord +…
  - effect: Update both base schema DDL and migration functions when int…
- Test files with staged removal (group 1 → group 3) should include TODO comments marking where assertions will be inverted, with clear indication of the current vs. future state. _(spec: 105_db_plan_state, confidence: 0.60)_
  - cause: Audit retention logic should preserve agent trace files (age…
- Test specs should be marked as passing (checked) only after all assertions exist and tests run to completion, even if implementation is incomplete. _(spec: 105_db_plan_state, confidence: 0.60)_
  - effect: JSONL trace files should be flushed immediately after each w…
- Import statements in test files should be ordered with module imports before local package imports, maintaining consistency with linter expectations. _(spec: 103_remove_hook_runner, confidence: 0.60)_
  - cause: When moving test files during a module relocation, ensure al…

## Anti-Patterns

- A constant-vector embedder (returning the same embedding for all inputs) makes similarity comparisons non-discriminative and can render tests unpassable when they depend on distinguishing between different semantic groups. _(spec: 111_rich_memory_rendering, confidence: 0.90)_
  - effect: The filter_ignored function uses strict greater-than (>) com…
- Avoid overwriting active spec metadata when also scanning archived directories; use early continue to skip archived entries if their name already exists in the known_specs map. _(spec: 111_rich_memory_rendering, confidence: 0.90)_
  - cause: When scanning multiple directories for specs, prioritize act…
- Findings must reference specific requirements, code artifacts, or observable facts; vague observations like 'consider adding more tests' are not valid findings and should be omitted. _(spec: 97_archetype_modes, confidence: 0.90)_
- Vague observations like 'consider adding more tests' are explicitly not findings in any reviewer mode; findings must be structured, evidence-based, and reference specific artifacts (spec IDs, code paths, requirement refs). _(spec: 97_archetype_modes, confidence: 0.90)_
- Pre-computed fact caches become stale and expensive to maintain during multi-step refactors; prefer live per-session computation with fallback mechanisms when retrieval infrastructure changes. _(spec: 104_adaptive_retrieval, confidence: 0.90)_
  - effect: When refactoring knowledge retrieval, consolidate multiple s…
- Test code should never be refactored for DRYness; tests should be verbose, explicit, and independently readable, even if that means duplicating setup across tests. _(spec: 103_remove_hook_runner, confidence: 0.90)_
  - cause: For code simplification, structure the refactoring plan into…

## Fragile Areas

- When using batch embeddings across multiple pipeline stages, embedder calls are sequential and indices carry state across stages—order matters for deterministic test outcomes. _(spec: 111_rich_memory_rendering, confidence: 0.90)_
  - effect: Test comments describing intended behavior (e.g., 'orthogona…
- .gitignore rules that exclude certain files from general ignores (via `!` prefix) become fragile when those files are later removed; update .gitignore atomically with code removal. _(spec: 104_adaptive_retrieval, confidence: 0.90)_
  - cause: When removing path constants from a centralized module, inli…
- Update test assertions that check schema version counts or migration history whenever new migrations are added, to ensure tests remain aligned with the actual migration registry. _(spec: 104_adaptive_retrieval, confidence: 0.90)_
  - cause: Maintain the base schema DDL in a separate constant that mir…
- Version numbers must be kept in sync across multiple files: __init__.py, pyproject.toml, and uv.lock to maintain consistency. _(spec: 103_agent_conversation_trace, confidence: 0.90)_
  - effect: Release candidates should use semantic versioning format wit…
- Version strings must be kept synchronized across multiple files: __init__.py, pyproject.toml, and uv.lock to maintain consistency. _(spec: 103_agent_conversation_trace, confidence: 0.90)_
  - effect: Pre-release versions use the format 'X.Y.ZrcN' (e.g., '3.0.0…
- Elixir's nested defmodule support requires special handling in the analyzer to correctly extract qualified module names and maintain containment relationships. _(spec: 107_additional_language_analyzers, confidence: 0.60)_
  - cause: Language analyzers should implement the LanguageAnalyzer pro…
- Documentation files (like memory.md) that reference version examples should be updated whenever version numbers change to maintain accuracy and consistency. _(spec: 107_additional_language_analyzers, confidence: 0.60)_
  - cause: Version numbers must be synchronized across multiple files: …
  - cause: Pre-release versions use the semantic versioning format 'X.Y…
- The _one_line_summary helper truncates the first line of content to 80 characters and appends '...' if longer, which can be problematic for very short lines but works as intended for typical markdown. _(spec: 104_adaptive_retrieval, confidence: 0.60)_
