## agent-fox configuration
## Uncomment and edit values to customize.

[orchestrator]
## Maximum parallel sessions (1-8, default: 2)
parallel = 3
## Shell command to run after each coder session (default: "")
quality_gate = "make test"
## Maximum USD spend per session, 0 = unlimited (default: 8.0)
max_budget_usd = 8.0
## Abort if more than 40% of tasks are blocked
max_blocked_fraction = 0.4

[models]
## Model tier for coding tasks (default: "ADVANCED")
coding = "STANDARD"

[archetypes]
## Enable coder archetype (default: true)
coder = true
## Enable reviewer archetype (replaces skeptic, oracle, auditor; default: true)
reviewer = true
## Enable verifier archetype (default: true)
verifier = true

[archetypes.instances]
## Number of verifier instances (max 1, default: 1)
verifier = 1

[archetypes.thinking.coder]
mode = "adaptive"
budget_tokens = 32000

[archetypes.thinking.verifier]
mode = "adaptive"
budget_tokens = 32000

[platform]
type = "github"

[night_shift]
hunt_scan_interval = 14400
